<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title><![CDATA[N神的研究所]]></title>
  <link href="http://nshen.net/atom.xml" rel="self"/>
  <link href="http://nshen.net"/>
  <updated>2013-07-14T06:59:06.321Z</updated>
  <id>http://nshen.net/</id>
  <author>
    <name><![CDATA[Nshen]]></name>
    <email><![CDATA[nshen121@gmail.com]]></email>
  </author>
  <generator uri="http://zespia.tw/hexo">Hexo</generator>
  <entry>
    <title type="html"><![CDATA[更换博客系统]]></title>
    <link href="http://nshen.net/article/2013-06-21/hello-world/"/>
    <id>http://nshen.net/article/2013-06-21/hello-world/</id>
    <published>2013-06-21T05:30:20.000Z</published>
    <updated>2013-07-14T06:06:07.000Z</updated>
    <content type="html"><![CDATA[<h3>2013-06-21新站借<a href="http://zespia.tw/hexo/" title="Hexo">Hexo</a>,全静态重生，手动把老文全转成Markdown了。</h3>
<a name="more"></a>

<p><img src="/image/0001.jpg" alt=""> </p>
<hr>
<h3>2011-08-03 新站搬到github,网站暂时算是复活了</h3>
<p>如果你是从搜索引擎跳转过来的，请到旧站备份中搜索你要的内容%  —&gt; <a href="http://www.riabook.cn/blog" target="_blank">[旧站的备份]</a></p>
<p>之前的服务器由于各种原因不能访问了，而且之前blog太老旧了，正好把博客换新了。 </p>
<p>前几天在<a href="http://junnan.org/blog/1350.htm" target="_blank">Bindiry的博客上</a>了解到可以在Github上用<a href="https://github.com/mojombo/jekyll" target="_blank">jekyll模板引擎</a>搭建自己的站点。
于是我就开始了折腾。。重新仔细学习了一遍Git，装ruby、devkit、jekyll，弄了好几天就是不支持中文，之后放弃，过了几天想起来了又google发现是ruby 1.9.2的问题，又全卸载，全重装。由于是样式盲，直接clone了<a href="http://blog.leezhong.com" target="_blank">这个博客</a>，页面非常清爽，很满意。
现在算是阶段性折腾完毕，所有东西对我都是新的，写日志都得用<a href="http://www.textism.com/tools/textile/index.php" target="_blank">Textile</a>，不知道这东西有没有本地的所见所得的编辑器，提交日志用git commit，正好可以练习git，现在还不知道能否习惯下来。
感谢上边提到的各位让我省了空间的钱。</p>
<p>自动语法高亮 ：</p>
<pre><code><figure class="highlight"><pre><span class="keyword">package</span> net.nshen.blog
{
    <span class="keyword">import</span> flash.display.Sprite;

    <span class="javadoc">/**
     * 复活咯～
     * <span class="javadoctag">@author</span>    Nshen
     */</span>
    <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Post</span> <span class="keyword">extends</span> <span class="title">Sprite</span>
    {</span>
        <span class="comment">// This is a String</span>
        <span class="keyword">private</span> var _str:String = <span class="string">"I'm the first post"</span>;

        <span class="javadoc">/**
         *  override this~
         */</span>
        <span class="keyword">public</span> function create():<span class="keyword">void</span>
        {
            trace(_str);
        }
    }
}
</pre></figure></code></pre>
]]></content>
  </entry>
  <entry>
    <title type="html"><![CDATA[从 ActionScript3 到 TypeScript]]></title>
    <link href="http://nshen.net/article/2013-05-18/as3-to-typescript/"/>
    <id>http://nshen.net/article/2013-05-18/as3-to-typescript/</id>
    <published>2013-05-18T06:56:31.000Z</published>
    <updated>2013-06-24T11:28:33.000Z</updated>
    <content type="html"><![CDATA[<p>以下是我总结的笔记，只涉及语法部分，有错漏请指正。</p>
<a name="more"></a>

<hr>
<h3>基本类型</h3>
<p>基础类型只有这么几种，与AS3对比</p>
<table>
  <tr><td><B>TypeScript</B></td><td><B>ActionScript3</B></td></tr>
  <tr><td>number</td><td>Number</td></tr>
  <tr><td>string</td><td>String</td></tr>
  <tr><td>boolean(0.8以前为bool)</td><td>Boolean</td></tr>
  <tr><td>any</td><td>*</td></tr>
  <tr><td>undefined</td><td>undefined</td></tr>
  <tr><td>null</td><td>null</td></tr>
</table>

<p>任意类型：</p>
<p>ActionScript3 ：</p>
<pre><code><figure class="highlight"><pre>var anyType : * = <span class="keyword">...</span>;
</pre></figure></code></pre>
<p>TypeScript：</p>
<pre><code><figure class="highlight"><pre>var anyType : any = <span class="keyword">...</span>;
</pre></figure></code></pre>
<hr>
<h3>变量修饰符：</h3>
<p>TS类似AS3 但不用写&quot;var&quot; ， 没有protected (未来也许会添加)，默认为public。</p>
<p>ActionScript3 ：</p>
<pre><code><figure class="highlight"><pre><span class="title">private</span> var str: String = <span class="string">"abc"</span>;    
<span class="title">public</span>  var num: Number = <span class="number">123</span>;     
<span class="title">var</span> num2 :Number = <span class="number">456</span>;        
<span class="title">static</span> var bo: Boolean = <span class="built_in">true</span>;    
<span class="title">public</span> var createTime: String;  
</pre></figure></code></pre>
<p>TypeScript:</p>
<pre><code><figure class="highlight"><pre> <span class="keyword">private</span> str: <span class="keyword">string</span> = <span class="string">"abc"</span>;    <span class="comment">// private property</span>
 <span class="keyword">public</span>  num: number = <span class="number">123</span>;      <span class="comment">// public property</span>
 num2 :number = <span class="number">456</span>;             <span class="comment">// 不写默认也为public</span>
 <span class="keyword">static</span> bo: boolean = <span class="keyword">true</span>;         <span class="comment">// static</span>
 <span class="keyword">public</span> createTime: <span class="keyword">string</span>;      <span class="comment">//createTime 类型为string，值为 undefined</span>
</pre></figure></code></pre>
<hr>
<h3>变量类型转换:</h3>
<p>ActionScript3 ：</p>
<pre><code><figure class="highlight"><pre><span class="title">var</span> str:String = <span class="string">"abc"</span>;
<span class="title">var</span> strNum:Number = Number(str);
<span class="title">var</span> strNum:Number = str as Number;
</pre></figure></code></pre>
<p>TypeScript ：</p>
<pre><code><figure class="highlight"><pre> <span class="keyword">var</span> str : string = <span class="string">"abc"</span>;
 <span class="keyword">var</span> strNum: number = <span class="xml"><span class="tag">&lt;<span class="title">number</span>&gt;</span> str; //编译报错 Cannot convert 'string' to 'number'</span>
</pre></figure></code></pre>
<p>发现使用any类型会编译通过：</p>
<pre><code><figure class="highlight"><pre> <span class="keyword">var</span> str2 :any = <span class="string">"abc"</span>; <span class="comment">//any类型</span>
 <span class="keyword">var</span> strNum2 :number = <span class="xml"><span class="tag">&lt;<span class="title">number</span>&gt;</span> str2; //通过
 strNum2 += 5; // abc5 </span>
</pre></figure></code></pre>
<p>用 instanceof 判断类型</p>
<pre><code><figure class="highlight"><pre> <span class="comment">// instanceof</span>
 <span class="function"><span class="keyword">function</span> <span class="title">CalculateArea</span><span class="params">(shape : Shape)</span> : <span class="title">number</span> {</span>
     <span class="keyword">if</span> (shape <span class="keyword">instanceof</span> Square) {
     <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="title">Square</span>&gt;</span>shape).x * (<span class="tag">&lt;<span class="title">Square</span>&gt;</span>shape).y;
 }
 if (shape instanceof Ellipse) {
     return (<span class="tag">&lt;<span class="title">Ellipse</span>&gt;</span>shape).r1 * (<span class="tag">&lt;<span class="title">Ellipse</span>&gt;</span>shape).r2 * Math.PI;
 }
 if (shape instanceof Triangle) {
     return 0.5 * (<span class="tag">&lt;<span class="title">Triangle</span>&gt;</span>shape).x * (<span class="tag">&lt;<span class="title">Triangle</span>&gt;</span>shape).y;
 }
 throw new TypeError("Unsupported type!");
}</span>
</pre></figure></code></pre>
<hr>
<h3>数组</h3>
<p>Typescript数组的写法比AS3漂亮很多</p>
<p>ActionScript3 ：</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> arr:Array = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="string">"a"</span>,<span class="string">"b"</span>,<span class="string">"c"</span>]; <span class="comment">// 任意类型数组</span>
<span class="keyword">var</span> strArray:Vector.<span class="xml"><span class="tag">&lt;<span class="title">String</span>&gt;</span> = Vector.<span class="tag">&lt;<span class="title">String</span>&gt;</span>(["a", "b", "c"]); //固定类型数组</span>
</pre></figure></code></pre>
<p>TypeScript ：</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> arr: any[] = <span class="keyword">new</span> Array(); <span class="comment">// 任意类型数组</span>
<span class="keyword">var</span> strArr: <span class="keyword">string</span>[] = [<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span> ]; <span class="comment">//固定类型数组</span>
</pre></figure></code></pre>
<p>二维数组</p>
<pre><code><figure class="highlight"><pre>var array2d: string[][] = [
[<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>],
[<span class="string">"x"</span>, <span class="string">"y"</span>, <span class="string">"z"</span>]
]<span class="comment">;</span>

// <span class="keyword">or</span>

var array2d: string[][] = new Array()<span class="comment">;</span>
array2d<span class="preprocessor">.push</span>([<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>])<span class="comment">;</span>
array2d<span class="preprocessor">.push</span>([<span class="string">"x"</span>, <span class="string">"y"</span>, <span class="string">"z"</span>])<span class="comment">;</span>
</pre></figure></code></pre>
<hr>
<h3>Object Types</h3>
<p>当AS3调用这样一个函数时，会有下面一个问题。</p>
<p>ActionScript3 ：</p>
<pre><code><figure class="highlight"><pre><span class="function"><span class="keyword">function</span> <span class="title">CalculateArea</span> <span class="params">( rect : Object )</span><span class="type">:Number</span>
{</span>
      <span class="keyword">return</span> rect.width * rect.height;
}
</pre></figure></code></pre>
<p>此时编译器并不知道rect这个Object里到底有没有width和height，只能等到runtime时才会知道。</p>
<p>TypeScript 引入<strong>Object Types</strong>解决这一问题，可以指定Object参数的具体内容，相当于让Object参数实现了interface</p>
<pre><code><figure class="highlight"><pre><span class="function"><span class="keyword">function</span> <span class="title">CalculateArea</span><span class="params">(rect: {width: number; height: number;})</span><span class="type">: number</span>
{</span>
    <span class="keyword">return</span> rect.width * rect.height;
}
</pre></figure></code></pre>
<p>此时如果调用<strong>CalculateArea({w:123,h:456});</strong>  编译器不会通过。</p>
<p><strong>Object Types</strong> 还支持”?&quot;表示可选参数</p>
<pre><code><figure class="highlight"><pre><span class="function"><span class="keyword">function</span> <span class="title">CalculateArea</span><span class="params">(rect: {width:number; height:number; depth?:number;})</span><span class="type">: number</span>
{</span>
    <span class="keyword">if</span>(rect.depth)
    {
        <span class="keyword">return</span> rect.width * rect.height * rect.depth;
    }
    <span class="keyword">return</span> rect.width * rect.height;
}

CalculateArea({w:<span class="number">123</span>,h:<span class="number">456</span>}); <span class="comment">//编译器报错</span>
CalculateArea({width:<span class="number">123</span>,height:<span class="number">456</span>}); <span class="comment">// 通过</span>
CalculateArea({width:<span class="number">123</span>,height:<span class="number">456</span>,depth:<span class="number">789</span>}); <span class="comment">// 通过</span>
</pre></figure></code></pre>
<hr>
<p>ActionScript3 ：</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> fun:<span class="keyword">Function</span>;   
<span class="keyword">var</span> fun2:<span class="function"><span class="keyword">Function</span> = <span class="title">someFunction</span>;</span>
</pre></figure></code></pre>
<p>TypeScript 可以指定Function需要的参数和返回值类型 ，所以变成了这样</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> fun : (str: <span class="keyword">string</span>) =&gt; <span class="keyword">void</span>;   <span class="comment">// fun是输入为string，没有输出的函数</span>
<span class="keyword">var</span> fun2 : (num: number) =&gt; number = someFnction;  <span class="comment">//someFnction函数必须输入输出都为number类型</span>
</pre></figure></code></pre>
<p>eg.指定callback函数为string输入，any输出。</p>
<pre><code><figure class="highlight"><pre><span class="keyword">function</span> vote(candidate: string, callback: (result: string) =&gt; any) {
// <span class="keyword">...</span>
} 

vote(<span class="string">"BigPig"</span>,
    <span class="keyword">function</span>(result: string) {
        <span class="keyword">if</span> (result === <span class="string">"BigPig"</span>) {
            // <span class="keyword">...</span>
        }
    });
</pre></figure></code></pre>
<p>TypeScript 这样的语法虽然使Function更清晰了，但也带来麻烦，比如一个简单的输入输出都是string的函数就要写好长不易阅读</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> sayHello: (input: string) =&gt; string = <span class="function"><span class="keyword">function</span> <span class="params">(s: string)</span> {</span>
    <span class="keyword">return</span> <span class="string">"Hello "</span> + s;
}

<span class="comment">//保存函数的数组也写很长</span>
<span class="keyword">var</span> strArray: { (s: string): string; }[] = [sayHello, <span class="function"><span class="keyword">function</span> <span class="title">aa</span><span class="params">(str: string)</span> {</span> <span class="keyword">return</span> str; }];    <span class="comment">//两个输入输出都为string的函数</span>
<span class="keyword">var</span> strArray: { (s: string) =&gt; string; }[]   <span class="comment">//这样写会报错，不知道为什么要把 “=&gt;”换成“ ：” 不太清楚为什么 -_-b</span>
</pre></figure></code></pre>
<p>所以引入了 函数接口</p>
<h3>函数接口</h3>
<pre><code><figure class="highlight"><pre><span class="comment">//定义输入输出都是字符串的函数的接口</span>
<span class="class"><span class="keyword">interface</span> <span class="title">IStringFunction</span> {</span>
    (input: <span class="keyword">string</span>) : <span class="keyword">string</span>;
}
</pre></figure></code></pre>
<p>上边很长的都可以这么写了</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> sayHello: IStringFunction = <span class="function"><span class="keyword">function</span> <span class="params">(s: <span class="keyword">string</span>)</span> <span class="comment">{
    return "Hello " + s;
}</span>
<span class="title">var</span> <span class="title">strArray</span>:</span> IStringFunction[];
strArray.push(sayHello);
</pre></figure></code></pre>
<h3>可选参数加&quot;?&quot;</h3>
<pre><code><figure class="highlight"><pre><span class="comment">//带默认值</span>
<span class="function"><span class="keyword">function</span> <span class="title">func</span> <span class="params">(a: number, b?: bool = false)</span><span class="type">: number</span>
 {</span>
    <span class="keyword">if</span> (b) { <span class="keyword">return</span> a + b };
    <span class="keyword">return</span> a;
}

<span class="comment">//不带默认值，要自己判断了</span>
<span class="function"><span class="keyword">function</span> <span class="title">func</span> <span class="params">(a: number,b?: bool)</span><span class="type">: number</span> 
{</span>
  <span class="keyword">if</span> ( b !== <span class="literal">null</span> &amp;&amp; b !== <span class="literal">undefined</span> )
  {
    <span class="keyword">if</span> ( b) { <span class="keyword">return</span> a + b };
   }
   <span class="keyword">return</span> a;
}
</pre></figure></code></pre>
<h3>rest参数 ...paramName[:paramType]</h3>
<pre><code><figure class="highlight"><pre><span class="function"><span class="keyword">function</span> <span class="title">CountDwarvesTallerThan</span><span class="params">(minHeight: number, <span class="rest_arg">...dwarves</span>: Dwarf[])</span> <span class="type">: number</span> 
{</span>
    <span class="keyword">var</span> count: number = <span class="number">0</span>;
    <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; dwarves.length; i++) {
        <span class="keyword">if</span> (dwarves[i].height &gt; minHeight) {
            count++;
        }
    }
    <span class="keyword">return</span> count;
}
</pre></figure></code></pre>
<hr>
<h3>Arrow Function</h3>
<p>关于this作用域的问题，AS1时代经常用到的一个技巧：</p>
<blockquote>
<p>var _this = this</p>
</blockquote>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> messenger = {
    message: <span class="string">"Hello World"</span>,
    start: <span class="keyword">function</span>() {
    <span class="keyword">var</span> _<span class="keyword">this</span> = <span class="keyword">this</span>;
    setTimeout(<span class="keyword">function</span>() { 
        alert(_<span class="keyword">this</span>.message); 
        }, <span class="number">3000</span>);
    }
};
messenger.start();
</pre></figure></code></pre>
<p>TypeScript把这个技巧封装到语言里了，叫<strong>Arrow Function</strong></p>
<p>语法格式为 ()=&gt;{}，例子：</p>
<p>TypeScript：</p>
<pre><code><figure class="highlight"><pre>var messenger = {
    message: <span class="string">"Hello World"</span>,
    start: function() {
        setTimeout(() =&gt; { alert(this<span class="preprocessor">.message</span>)<span class="comment">; }, 3000);</span>
    }
}<span class="comment">;</span>
messenger<span class="preprocessor">.start</span>()<span class="comment">;</span>
</pre></figure></code></pre>
<p>编译后的JavaScript跟上边的一样</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> messenger = {
    message: <span class="string">"Hello World"</span>,
    start: <span class="function"><span class="keyword">function</span> <span class="params">()</span> {</span>
        <span class="keyword">var</span> _<span class="keyword">this</span> = <span class="keyword">this</span>;
        setTimeout(<span class="function"><span class="keyword">function</span> <span class="params">()</span> {</span>
            alert(_<span class="keyword">this</span>.message);
        }, <span class="number">3000</span>);
    }
};
messenger.start();
</pre></figure></code></pre>
<p>网上找到的一个例子，</p>
<pre><code><figure class="highlight"><pre><span class="keyword">declare</span> <span class="keyword">var</span> menu: HTMLElement;
<span class="keyword">declare</span> <span class="keyword">var</span> sideBar: HTMLElement;

<span class="class"><span class="keyword">class</span> <span class="title">UITester</span> {</span>
    menuTouches: number;
    sidebarTouches: number;

    beginMenuTest(): void {
        <span class="keyword">this</span>.menuTouches = <span class="number">0</span>;   <span class="comment">// Right!!</span>
        menu.onmouseenter = <span class="function"><span class="keyword">function</span> <span class="params">(e)</span> {</span>
            <span class="keyword">this</span>.menuTouches++;  <span class="comment">// Wrong!! </span>
        }
    }

    beginSidebarTest(): void {
        <span class="keyword">this</span>.sidebarTouches = <span class="number">0</span>;  <span class="comment">// Right!!</span>
        sideBar.onmousemove = e =&gt; {  
            <span class="keyword">this</span>.sidebarTouches++;  <span class="comment">// Still right!!</span>
        }
    }
}
</pre></figure></code></pre>
<p>语法中用了(e)=&gt;{} ,将e传给后边的函数，并且省略了e的括号，会编译成这样：</p>
<pre><code><figure class="highlight"><pre>var UITester = (<span class="function"><span class="keyword">function</span> <span class="params">()</span> {</span>
    <span class="function"><span class="keyword">function</span> <span class="title">UITester</span><span class="params">()</span> { }</span>
    <span class="transposed_variable">UITester.</span><span class="transposed_variable">prototype.</span>beginMenuTest = <span class="function"><span class="keyword">function</span> <span class="params">()</span> {</span>
        <span class="transposed_variable">this.</span>menuTouches = <span class="number">0</span>;
        <span class="transposed_variable">menu.</span>onmouseenter = <span class="function"><span class="keyword">function</span> <span class="params">(e)</span> {</span>
            <span class="transposed_variable">this.</span>menuTouches++;
        };
    };
    <span class="transposed_variable">UITester.</span><span class="transposed_variable">prototype.</span>beginSidebarTest = <span class="function"><span class="keyword">function</span> <span class="params">()</span> {</span>
        var _this = this;
        <span class="transposed_variable">this.</span>sidebarTouches = <span class="number">0</span>;
        <span class="transposed_variable">sideBar.</span>onmousemove = <span class="function"><span class="keyword">function</span> <span class="params">(e)</span> {</span>
            <span class="transposed_variable">_this.</span>sidebarTouches++;
        };
    };
    <span class="keyword">return</span> UITester;
})();
</pre></figure></code></pre>
<p>这里有篇教程详细解释了这个语法：</p>
<blockquote>
<p><a href="http://www.codebelt.com/typescript/arrow-function-typescript-tutorial/">http://www.codebelt.com/typescript/arrow-function-typescript-tutorial/</a></p>
</blockquote>
<hr>
<h3>类相关的</h3>
<p>TypeScript中的module相当于ActionScript3中的Package</p>
<p>TypeScript中构造函数的函数名用<strong>constructor</strong> ，而不用类名。</p>
<p>TypeScript:</p>
<pre><code><figure class="highlight"><pre>module net<span class="variable">.nshen</span> { 
    <span class="keyword">export</span> <span class="class"><span class="keyword">class</span> <span class="id">Test1</span></span>
    {
        <span class="keyword">private</span> str: string = <span class="string">"abc"</span>;    <span class="comment">// private property</span>
        <span class="keyword">public</span>  num: number = <span class="number">123</span>;      <span class="comment">//public property</span>

        <span class="keyword">public</span> createTime: string;      <span class="comment">//createTime = undefined</span>

        constructor() <span class="comment">// constructor</span>
        {
            <span class="keyword">this</span><span class="variable">.createTime</span> = new Date()<span class="variable">.toUTCString</span>();
        }

        <span class="keyword">static</span> traceDate(): <span class="keyword">void</span>
        {
            var currentDate: Date = new Date(); 
            console<span class="variable">.log</span>(currentDate<span class="variable">.toUTCString</span>());
        }
    }
}
</pre></figure></code></pre>
<p>调用Static方法</p>
<pre><code><figure class="highlight"><pre>net<span class="preprocessor">.nshen</span><span class="preprocessor">.Test</span>1<span class="preprocessor">.traceDate</span>()<span class="comment">;</span>
</pre></figure></code></pre>
<h3>module原理</h3>
<p>module始终是要编译成JS代码的，写一个简单的module看一下原理：</p>
<pre><code><figure class="highlight"><pre>module M {
    <span class="keyword">var</span> s = <span class="string">"hello"</span>;
    export <span class="function"><span class="keyword">function</span> <span class="title">f</span><span class="params">()</span> {</span>
        <span class="keyword">return</span> s;
     }
}

M.f();
M.s;  <span class="comment">// Error, s is not exported</span>
</pre></figure></code></pre>
<p>编译后的JS代码</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> M;
(<span class="keyword">function</span>(M) {
    <span class="keyword">var</span> s = <span class="string">"hello"</span>;
    <span class="function"><span class="keyword">function</span> <span class="title">f</span><span class="params">()</span> {</span>
    <span class="keyword">return</span> s;
    }
    M.f = f;
})(M||(M={}));
</pre></figure></code></pre>
<p>据说这是js界很流行的写法，叫做<strong>JavaScript module pattern</strong></p>
<hr>
<h3>函数重载</h3>
<p>AS3和JS都是不支持函数重载的，TypeScript以一种鸡肋的方式支持着。</p>
<p>先写一些同名的函数声明，最后在一个同名函数里写出实现（要自己判断参数类型）：</p>
<p>TypeScript:</p>
<pre><code><figure class="highlight"><pre><span class="function"><span class="keyword">function</span> <span class="title">attr</span><span class="params">(name: string)</span><span class="type">: string</span>;</span>
<span class="function"><span class="keyword">function</span> <span class="title">attr</span><span class="params">(name: string, value: string)</span><span class="type">: Accessor</span>;</span>
<span class="function"><span class="keyword">function</span> <span class="title">attr</span><span class="params">(map: any)</span><span class="type">: Accessor</span>;</span>

<span class="function"><span class="keyword">function</span> <span class="title">attr</span><span class="params">(nameOrMap: any, value?: string)</span><span class="type">: any</span> {</span>
    <span class="keyword">if</span> (nameOrMap &amp;&amp; <span class="keyword">typeof</span> nameOrMap === <span class="string">"object"</span>) {
        <span class="comment">// handle map case</span>
    }
    <span class="keyword">else</span> {
        <span class="comment">// handle string case</span>
    }
}
</pre></figure></code></pre>
<p>最终会编译成一个JS方法：</p>
<p>JavaScript：</p>
<pre><code><figure class="highlight"><pre><span class="function"><span class="keyword">function</span> <span class="title">attr</span><span class="params">(nameOrMap, value)</span> {</span>
    <span class="keyword">if</span> (nameOrMap &amp;&amp; <span class="keyword">typeof</span> nameOrMap === <span class="string">"object"</span>) {
    } <span class="keyword">else</span> {
    }
}
</pre></figure></code></pre>
<p>TypeScript 允许多个类在同一个文件里，但如果类与类在不同的文件，需要这种写法，相当于AS3 的 import</p>
<pre><code><figure class="highlight"><pre><span class="comment">/// &lt;reference path="SimpleWebSocket.ts"/&gt;</span>
<span class="class"><span class="keyword">class</span> <span class="title">ComplexWebSocket</span> <span class="keyword">extends</span> <span class="title">SimpleWebSocket</span> {</span>
...
}
</pre></figure></code></pre>
<p>override方法子类不需要写关键字，直接同名方法即可 ，可调用super.xxx()</p>
<pre><code><figure class="highlight"><pre><span class="class"><span class="keyword">class</span> <span class="title">Base</span> {</span>

    <span class="keyword">public</span> test():number
    {
        <span class="keyword">return</span> <span class="number">1</span>;
    }

    <span class="keyword">public</span> test2():number
    {
        <span class="keyword">return</span> <span class="number">2</span>;
    }
}

<span class="class"><span class="keyword">class</span> <span class="title">Derived</span> <span class="keyword">extends</span> <span class="title">Base</span> {</span>

    <span class="keyword">public</span> test():number
    {
        <span class="keyword">return</span> <span class="number">3</span>;
    }

    <span class="keyword">public</span> test2():number
    {
        <span class="keyword">return</span> <span class="keyword">super</span>.test();
    }

}


var d:Derived = <span class="keyword">new</span> Derived();
console.log(d.test()); <span class="comment">// 3</span>
console.log(d.test2());<span class="comment">// 1</span>
</pre></figure></code></pre>
<hr>
<h3>Enum</h3>
<p>TypeScript支持enum关键字</p>
<pre><code><figure class="highlight"><pre><span class="keyword">enum</span> <span class="title">Color</span> { Red, Green, Blue }
console.<span class="keyword">log</span>(Color.Red); <span class="comment">// 0</span>
var c:number = Color.Green;
console.<span class="keyword">log</span>(Color[c])  <span class="comment">//Green</span>
</pre></figure></code></pre>
<p>生成对应的js</p>
<pre><code><figure class="highlight"><pre>var <span class="keyword">Color</span>;
(function (<span class="keyword">Color</span>) {
    <span class="keyword">Color</span>[<span class="keyword">Color</span>[<span class="string">"Red"</span>] = <span class="number">0</span>] = <span class="string">"Red"</span>;
    <span class="keyword">Color</span>[<span class="keyword">Color</span>[<span class="string">"Green"</span>] = <span class="number">1</span>] = <span class="string">"Green"</span>;
    <span class="keyword">Color</span>[<span class="keyword">Color</span>[<span class="string">"Blue"</span>] = <span class="number">2</span>] = <span class="string">"Blue"</span>;
})(<span class="keyword">Color</span> || (<span class="keyword">Color</span> = {}));
console.log(<span class="keyword">Color</span>.Red);
var c = <span class="keyword">Color</span>.Green;
console.log(<span class="keyword">Color</span>[c]);//Green
</pre></figure></code></pre>
]]></content>
    <category scheme="http://nshen.net/categories/article/" term="article"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[笔记《黑客与画家》如何创造财富]]></title>
    <link href="http://nshen.net/article/2012-04-09/howToMakeMoney/"/>
    <id>http://nshen.net/article/2012-04-09/howToMakeMoney/</id>
    <published>2012-04-09T06:56:31.000Z</published>
    <updated>2013-06-21T08:49:25.000Z</updated>
    <content type="html"><![CDATA[<h3>如何创造财富</h3>
<p>《黑客与画家》第8章笔记
<a name="more"></a></p>
<p><a href="/image/howToMakeMoney.png" target="_blank" >图片过大，新窗口打开</a>.</p>

<p><img src="/image/howToMakeMoney.png" alt="如何创造财富"></p>
]]></content>
    <category scheme="http://nshen.net/categories/article/" term="article"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[跨IOS，Android的小游戏《阿鼻地狱》]]></title>
    <link href="http://nshen.net/project/2011-10-05/go-to-hell/"/>
    <id>http://nshen.net/project/2011-10-05/go-to-hell/</id>
    <published>2011-10-05T06:56:31.000Z</published>
    <updated>2013-06-22T04:48:29.000Z</updated>
    <content type="html"><![CDATA[<p>本是用来参加腾讯和天地会举办的flash手机游戏开发大赛的游戏。</p>
<a name="more"></a>

<p>使用Flixel游戏引擎开发，像素图+程序一手操办，AIR3.0打包，可以跨IOS，Android的。</p>
<p>当时在iphone4上测试基本维持在30帧。</p>
<hr>
<h3>2011-10-05发布剪辑的视频：</h3>
<embed src="http://player.youku.com/player.php/sid/XMzA5OTI2NDYw/v.swf" type="application/x-shockwave-flash" allowscriptaccess="always" allowfullscreen="true" wmode="opaque" width="610" height="500"></embed>

<hr>
<h3>2011-10-06发布iphone4实机测试视频:</h3>
<embed src="http://player.youku.com/player.php/sid/XMzEwMzE2Njky/v.swf" type="application/x-shockwave-flash" allowscriptaccess="always" allowfullscreen="true" wmode="opaque" width="610" height="500"></embed>


<hr>
<h3>实机拍摄环境比较简陋</h3>
<p>是用我老婆的g7手机拍的，效果不太好，没办法先这样吧</p>
<p><img src="/image/hell01.jpg" alt=""></p>
<hr>
<h3>概念涂鸦一张</h3>
<p><img src="/image/hell02.jpg" alt=""></p>
]]></content>
    <category scheme="http://nshen.net/tags/游戏/" term="游戏"/>
    <category scheme="http://nshen.net/categories/project/" term="project"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[AS3与数据结构]]></title>
    <link href="http://nshen.net/article/2011-08-25/as3-DataStructure/"/>
    <id>http://nshen.net/article/2011-08-25/as3-DataStructure/</id>
    <published>2011-08-25T06:56:31.000Z</published>
    <updated>2013-06-21T09:18:50.000Z</updated>
    <content type="html"><![CDATA[<p>时至今日国内外都还没有一本专讲AS3与数据结构的书，对于我这种非科班毕业的社会闲杂人等来说，入门数据结构太难了，我参考了各方代码，经过一段时间的恶补，整理了一下目前Flash开发中有可能遇得到的数据结构。完整代码在文章结尾有下载，如有错漏请直接指出谢谢。</p>
<a name="more"></a>

<h3>数组</h3>
<p>Array类应该是Flash里最常用的数据结构了。比其他语言的数组高级和灵活许多，随意装入任何数据类型，不用固定长度。
访问速度快，从后方添加(push)和删除(pop)快，但从中间或开头删除会很慢。</p>
<p>从100万长度的数组开头删除500个元素在我的机器上差不多要1秒钟。</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> arr:Array = []
<span class="keyword">for</span>(<span class="keyword">var</span> i:<span class="keyword">uint</span>= <span class="number">0</span> ;i&lt;<span class="number">1000000</span>;i++)arr[i]=i ;

<span class="keyword">var</span> startT:Number = getTimer()
<span class="keyword">var</span> num:<span class="keyword">uint</span> = <span class="number">500</span>
<span class="keyword">while</span>(num&gt;<span class="number">0</span>)
{
    arr.splice(<span class="number">0</span>,<span class="number">1</span>)
    num--
}
trace(<span class="string">"time:"</span>,getTimer() - startT)
</pre></figure></code></pre>
<p>但如果数组没有顺序的话，快速删除法1000个在我机器上才需要1毫秒。</p>
<pre><code><figure class="highlight"><pre>var arr:Array = <span class="matrix">[]</span>
<span class="keyword">for</span>(var <span class="built_in">i</span>:uint= <span class="number">0</span> ;<span class="built_in">i</span>&lt;<span class="number">1000000</span>;<span class="built_in">i</span>++)arr<span class="matrix">[i]</span>=<span class="built_in">i</span> ;

var startT:Number = getTimer()
var num:uint = <span class="number">1000</span>
<span class="keyword">while</span>(num&gt;<span class="number">0</span>)
<span class="cell">{
    arr[<span class="number">0</span>] =arr[arr.length-<span class="number">1</span>]
    arr.length -=<span class="number">1</span>
    num--
}</span>
trace(getTimer() - startT)
</pre></figure></code></pre>
<p>Vector：Vector是Array的升级版。如果想追求效率，恰巧数组中的元素类型是固定的，或者长度是固定的，就可以将Array换成Vector类提升效率。</p>
<h3>堆栈</h3>
<p>堆栈是先进后出的结构，像子弹梭子一样，由于数组的push() pop()非常快，所以数组本身就是最好的堆栈。</p>
<h3>队列</h3>
<p>队列是先进先出，数组也可以做队列，但是从数组前端删除数据比较慢，下边10万条数据我的机器运行差不多需要2秒。</p>
<pre><code><figure class="highlight"><pre>var start<span class="variable">T</span>:<span class="variable">Number</span> = get<span class="variable">Timer</span>()
var queue:<span class="variable">Array</span> = []
<span class="function"><span class="title">for</span><span class="params">(var i:int = <span class="number">0</span> ; i&lt;<span class="number">100000</span> ;i++)</span>
{
    <span class="title">queue</span>.<span class="title">push</span><span class="params">(i)</span>;
}
<span class="title">for</span><span class="params">(var i:int = <span class="number">0</span> ; i&lt;<span class="number">100000</span> ;i++)</span>
{
    <span class="title">queue</span>.<span class="title">shift</span><span class="params">()</span>
}
<span class="title">trace</span><span class="params">(<span class="function_name">getTimer</span>() - start<span class="variable">T</span>)</span>
</pre></figure></code></pre>
<p>我参考as3ds改写了个链表（DLinkedList） ，实现了数组一样的 push() pop() shift() unshift() 四个方法。用这个链表只需要改一行代码，同样操作在我的机器上只要100多毫秒。</p>
<pre><code><figure class="highlight"><pre>var start<span class="variable">T</span>:<span class="variable">Number</span> = get<span class="variable">Timer</span>()
var queue:<span class="variable">DLinkedList</span> = new <span class="variable">DLinkedList</span>()
<span class="function"><span class="title">for</span><span class="params">(var i:int = <span class="number">0</span> ; i&lt;<span class="number">100000</span> ;i++)</span>
{
    <span class="title">queue</span>.<span class="title">push</span><span class="params">(i)</span>;
}
<span class="title">for</span><span class="params">(var i:int = <span class="number">0</span> ; i&lt;<span class="number">100000</span> ;i++)</span>
{
    <span class="title">queue</span>.<span class="title">shift</span><span class="params">()</span>
}
<span class="title">trace</span><span class="params">(<span class="function_name">getTimer</span>() - start<span class="variable">T</span>)</span>
</pre></figure></code></pre>
<p>这个链表是一个双向链表，可以当作各种队列也可以当作堆栈，效率都不错。</p>
<h3>链表</h3>
<p>这个链表实现的是一个双向链表，因为删除数据会快些，单链表优点就是省一点点内存，但删除会慢很多，用处不大，所以没有实现。
我觉得除了把链表当成队列使用，平时几乎用不到。如果非要使用就要使用Iterator了，Iterator是一种设计模式，不懂可以搜索一下。
下边是对链表所有操作的演示。</p>
<pre><code><figure class="highlight"><pre>var l:DLinkedList = new DLinkedList()
l<span class="variable">.push</span>(<span class="number">1</span>)
l<span class="variable">.push</span>(<span class="number">2</span>)
l<span class="variable">.push</span>(<span class="number">3</span>)
trace(l,l<span class="variable">.head</span>,l<span class="variable">.tail</span>) <span class="comment">//[1,2,3] 1 3</span>
l<span class="variable">.pop</span>()
trace(l,l<span class="variable">.head</span>,l<span class="variable">.tail</span>) <span class="comment">//[1,2] 1 2</span>
l<span class="variable">.unshift</span>(<span class="number">0</span>)
trace(l,l<span class="variable">.head</span>,l<span class="variable">.tail</span>) <span class="comment">//[0,1,2] 0 2</span>
l<span class="variable">.shift</span>()
trace(l,l<span class="variable">.head</span>,l<span class="variable">.tail</span>) <span class="comment">//[1,2] 1 2</span>
l<span class="variable">.push</span>(<span class="number">3</span>)
trace(l,l<span class="variable">.head</span>,l<span class="variable">.tail</span>) <span class="comment">//[1,2,3] 1 3</span>

<span class="comment">//遍历,输出  1,2,3</span>
var it:DListIterator = l<span class="variable">.getIterator</span>()
<span class="keyword">for</span>(it<span class="variable">.start</span>() ; it<span class="variable">.hasNext</span>(); it<span class="variable">.next</span>())
{
    trace(it<span class="variable">.node</span><span class="variable">.data</span>)
}
it<span class="variable">.start</span>()  <span class="comment">// 索引归0</span>
it<span class="variable">.next</span>()   
l<span class="variable">.remove</span>(it) <span class="comment">//remove 2</span>
l<span class="variable">.remove</span>(it) <span class="comment">//remove 3</span>
trace(l,l<span class="variable">.head</span>,l<span class="variable">.tail</span>) <span class="comment">//[1] 1 1</span>
l<span class="variable">.insert</span>(it,<span class="number">2</span>) 
trace(l,l<span class="variable">.head</span>,l<span class="variable">.tail</span>) <span class="comment">//[1,2] 1 2</span>
l<span class="variable">.insert</span>(it,<span class="number">3</span>)
trace(l,l<span class="variable">.head</span>,l<span class="variable">.tail</span>) <span class="comment">//[1,3,2] 1 2</span>
</pre></figure></code></pre>
<h3>哈希表</h3>
<p>哈希表应该就是Object？ 在Flash里Object是动态的，赋值就 obj.abc = 123 ,删除就 delete obj.abc</p>
<pre><code><figure class="highlight"><pre><span class="keyword">var</span> o:Object = {}
o[<span class="string">"key"</span>] = obj   <span class="comment">//插入</span>
<span class="keyword">delete</span> o[<span class="string">"key"</span>]  <span class="comment">//删除</span>
</pre></figure></code></pre>
<p>如果追求Object做key可以用flash.utils.Dictionary类代替Object。</p>
<h3>树</h3>
<p>上边数组和链表都叫线性结构，树是典型的非线性数据结构，普通的树也不常用，但作为一个基础必须掌握，这里的树使用上边提到的双向链表存储子树。操作树要用TreeIterator，下边是演示。</p>
<pre><code><figure class="highlight"><pre><span class="title">var</span> tree:<span class="type">Tree</span> = new <span class="type">Tree</span>(<span class="number">0</span>);
<span class="title">var</span> itr:<span class="type">TreeIterator</span> = tree.getIterator()
<span class="title">itr</span>.appendChild(<span class="number">1</span>)
<span class="title">itr</span>.appendChild(<span class="number">2</span>) 

      <span class="number">0</span>  
     / \
    <span class="number">1</span>   <span class="number">2</span>


<span class="title">itr</span>.down()
<span class="title">itr</span>.appendChild(<span class="number">3</span>)


         <span class="number">0</span>  
        / \
       <span class="number">1</span>   <span class="number">2</span>
      /
     <span class="number">3</span>


<span class="title">itr</span>.up()
<span class="title">itr</span>.appendChild(<span class="number">4</span>)

          <span class="number">0</span>  
        / | \ 
       <span class="number">1</span>  <span class="number">2</span>  <span class="number">4</span>
      /
     <span class="number">3</span>


<span class="title">itr</span>.childEnd()
<span class="title">itr</span>.down()
<span class="title">itr</span>.prependChild(<span class="number">5</span>)
<span class="title">itr</span>.prependChild(<span class="number">6</span>)


        <span class="number">0</span>  
      / | \ 
     <span class="number">1</span>  <span class="number">2</span>  <span class="number">4</span>
    /      /\
   <span class="number">3</span>      <span class="number">6</span>  <span class="number">5</span>    
<span class="title">trace</span>(tree.dump())
</pre></figure></code></pre>
<p>上边的代码输出如下：</p>
<pre><code><figure class="highlight"><pre><span class="title">[</span><span class="comment">TreeNode</span> &gt;<span class="comment">(root)</span> <span class="comment">has</span> <span class="comment">2</span> <span class="comment">child</span> <span class="comment">nodes</span>, <span class="comment">data=room1</span>]
<span class="literal">+</span><span class="literal">-</span><span class="literal">-</span><span class="literal">-</span><span class="title">[</span><span class="comment">TreeNode</span> &gt; <span class="comment">has</span> <span class="comment">2</span> <span class="comment">child</span> <span class="comment">nodes</span>, <span class="comment">data=room2</span>]
<span class="comment">|</span>    <span class="literal">+</span><span class="literal">-</span><span class="literal">-</span><span class="literal">-</span><span class="title">[</span><span class="comment">TreeNode</span> &gt;<span class="comment">(leaf)</span>, <span class="comment">data=room4</span>]
<span class="comment">|</span>    <span class="literal">+</span><span class="literal">-</span><span class="literal">-</span><span class="literal">-</span><span class="title">[</span><span class="comment">TreeNode</span> &gt;<span class="comment">(leaf)</span>, <span class="comment">data=room5</span>]
<span class="literal">+</span><span class="literal">-</span><span class="literal">-</span><span class="literal">-</span><span class="title">[</span><span class="comment">TreeNode</span> &gt;<span class="comment">(leaf)</span>, <span class="comment">data=room3</span>]
</pre></figure></code></pre>
<p>我实现了一个toXML方法，如果把tree.toXML(tree)赋给Flex的Tree组件的dataProvider属性，就会是这个样子。</p>
<p><img src="/image/tree01.jpg" alt="/image/tree01.jpg"></p>
<p>树的遍历分为前序遍历和后序遍历，演示：</p>
<script type="text/javascript">swfobject.embedSWF("/image/TreeTraversal.swf","/image/TreeTraversal","600","380","9.0.0","/image/expressInstall.swf")</script><p><div id="/image/TreeTraversal"><br /><p><a href="http://www.adobe.com/go/getflashplayer"><img src="http://www.adobe.com/images/shared/download_buttons/get_flash_player.gif" alt="Get Adobe Flash player" /></a></p></p></div>



<p><a href="http://lab.polygonal.de/2007/05/15/data-structures-example-the-tree-class/" target="_blank">上边是直接来自as3ds的演示，了解更多点击这里</a> </p>
<p>代码实现看着很简单，但很需要费脑子的，是递归实现。</p>
<pre><code><figure class="highlight"><pre><span class="comment">/**
* 前序遍历
*/</span>
<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">preorder</span><span class="params">(Node:Tree,Process:Function)</span><span class="type">:void</span>
{</span>
    Process(Node)
    <span class="keyword">var</span> itr:DListIterator = Node.children.getIterator()
    <span class="keyword">while</span>(itr.hasNext())
    {
        preorder(Tree(itr.node.data),Process)
        itr.next();
    }
}

<span class="comment">/**
* 后序遍历
*/</span>
<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">postorder</span><span class="params">(Node:Tree,Process:Function)</span><span class="type">:void</span>
{</span>
    <span class="keyword">var</span> itr:DListIterator =  Node.children.getIterator()
    <span class="keyword">while</span>(itr.hasNext())
    {
        postorder(Tree(itr.node.data),Process)
    }
    Process(Node)

}
</pre></figure></code></pre>
<h3>二叉堆与优先队列</h3>
<p>优先队列是一种特殊的队列，它不是先入先出，而是入队的元素会自动按你指定顺序排列，先出队的永远是你指定的顺序里排最前面的。
当然这个优先队列可以用数组实现，每次入队就push后排一次序，出队pop就可以了，但是这样效率不佳，我们的优先队列是使用二叉堆实现的。</p>
<p>首先了解二叉树是只有2个子节点的树 ，二叉堆是一种特殊的二叉树，他的每个父节点都比子节点大。
里边用了些特殊的算法，让元素入队出队时效率更好。后边的A*寻路也用这个二叉堆来优化效率。具体算法见代码</p>
<pre><code><figure class="highlight"><pre>var priorityQueue:Heap =new Heap() <span class="comment">//新建二叉堆，默认从大到小排列</span>
priorityQueue<span class="variable">.enqueue</span>(<span class="number">2</span>) <span class="comment">//入队9个数字</span>
priorityQueue<span class="variable">.enqueue</span>(<span class="number">6</span>)
priorityQueue<span class="variable">.enqueue</span>(<span class="number">5</span>)
priorityQueue<span class="variable">.enqueue</span>(<span class="number">8</span>)            
priorityQueue<span class="variable">.enqueue</span>(<span class="number">7</span>)
priorityQueue<span class="variable">.enqueue</span>(<span class="number">4</span>)
priorityQueue<span class="variable">.enqueue</span>(<span class="number">1</span>)
priorityQueue<span class="variable">.enqueue</span>(<span class="number">3</span>)
priorityQueue<span class="variable">.enqueue</span>(<span class="number">9</span>)

trace(priorityQueue)  <span class="comment">//输出9,8,5,7,6,4,1,2,3   ，发现并不是按顺序排列的</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//9 但出队时就已经是排序好的了</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//8</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//7</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//6</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//5</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//4</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//3</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//2</span>
trace(priorityQueue<span class="variable">.dequeue</span>())  <span class="comment">//1</span>

<span class="comment">//如果想要逆序排列，只需要提供一个compare Function，这个跟数组sort方法里的compareFunction参数一样，如下：</span>

var f:Function = function(a:<span class="keyword">int</span>,b:<span class="keyword">int</span>):<span class="keyword">int</span>{<span class="keyword">return</span> b-a}
var p:Heap =new Heap(f)
p<span class="variable">.enqueue</span>(<span class="number">2</span>)
p<span class="variable">.enqueue</span>(<span class="number">6</span>)
p<span class="variable">.enqueue</span>(<span class="number">5</span>)
p<span class="variable">.enqueue</span>(<span class="number">8</span>)            
p<span class="variable">.enqueue</span>(<span class="number">7</span>)
p<span class="variable">.enqueue</span>(<span class="number">4</span>)
p<span class="variable">.enqueue</span>(<span class="number">1</span>)
p<span class="variable">.enqueue</span>(<span class="number">3</span>)
p<span class="variable">.enqueue</span>(<span class="number">9</span>)

trace(p<span class="variable">.dequeue</span>()) <span class="comment">//1</span>
trace(p<span class="variable">.dequeue</span>()) <span class="comment">//2</span>
trace(p<span class="variable">.dequeue</span>()) <span class="comment">//3</span>
trace(p<span class="variable">.dequeue</span>()) <span class="comment">//4</span>
trace(p<span class="variable">.dequeue</span>()) <span class="comment">//5</span>
trace(p<span class="variable">.dequeue</span>()) <span class="comment">//6</span>
trace(p<span class="variable">.dequeue</span>()) <span class="comment">//7</span>
trace(p<span class="variable">.dequeue</span>()) <span class="comment">//8</span>
trace(p<span class="variable">.dequeue</span>()) <span class="comment">//9</span>
</pre></figure></code></pre>
<p>其实Heap内部也是由数组存储的，它的算法有两个关键</p>
<p>1) 如何用数组储存二叉树结构</p>
<p>比如原树状结构为： </p>
<pre><code><figure class="highlight"><pre>      4
     / <span class="command">\
</span>    3   0
   /<span class="command">\
</span>  1  2
</pre></figure></code></pre>
<p>则存储成数组为：[4,3,0,1,2]</p>
<p>2) 如何永远保持parent比child大</p>
<p>这点比较麻烦具体可以参考我的代码，或者找本书看看。</p>
<h3>四叉树</h3>
<p>四叉树通常用于2d游戏碰撞检测。假设屏幕上有500个mc，每2个之间都要进行碰撞检测，那么每一个mc都要与其他499个mc进行一次碰撞检测，那每帧总共就接近做500×500 = 250000次检测。
如果是小球还好办，用半径法，如果碰上复杂的mc需要位图碰撞检测，那每帧检测25万次flash是吃不消的。
一个优化的办法就是只与有可能碰撞的mc进行碰撞检测，这时四叉树就派上用场了。</p>
<p>移动鼠标会发现，只有红色的方块是有可能与鼠标拖动的方块产生碰撞的，你只要将鼠标方块与红色的方块逐个进行检测就好了，大大提升了效率</p>
<script type="text/javascript">swfobject.embedSWF("/image/QuadTree_retrieve.swf","/image/QuadTree_retrieve","500","500","9.0.0","/image/expressInstall.swf")</script><p><div id="/image/QuadTree_retrieve"><br /><p><a href="http://www.adobe.com/go/getflashplayer"><img src="http://www.adobe.com/images/shared/download_buttons/get_flash_player.gif" alt="Get Adobe Flash player" /></a></p></p></div>


<p>四叉树首先通过一个建树的过程将屏幕上的物体分配到，左上，右上，左下，右下四个象限中，这四个象限对应树的四个节点，每个象限还可以继续分成四个象限，根据具体情况考虑分配几层达到一个查找最快效果。</p>
<p>对比一下速度，下边是数组遍历，250个Sprite，两两碰撞在我机器上就惨不忍睹了。 （放上鼠标开始）</p>
<script type="text/javascript">swfobject.embedSWF("/image/Array_collition.swf","/image/Array_collition","600","500","9.0.0","/image/expressInstall.swf")</script><p><div id="/image/Array_collition"><br /><p><a href="http://www.adobe.com/go/getflashplayer"><img src="http://www.adobe.com/images/shared/download_buttons/get_flash_player.gif" alt="Get Adobe Flash player" /></a></p></p></div>



<p>四叉树版本，500个Sprite，2倍精灵的数量两两碰撞仍然跑的流畅    （放上鼠标开始）</p>
<script type="text/javascript">swfobject.embedSWF("/image/QuadTree_collition.swf","/image/QuadTree_collition","600","500","9.0.0","/image/expressInstall.swf")</script><p><div id="/image/QuadTree_collition"><br /><p><a href="http://www.adobe.com/go/getflashplayer"><img src="http://www.adobe.com/images/shared/download_buttons/get_flash_player.gif" alt="Get Adobe Flash player" /></a></p></p></div>



<p>四叉树的思想就是先屏幕分割，然后过滤出离自己很近的，有可能产生碰撞的进行碰撞检测。Flash界使用四叉树最出名的应该就是<a href="http://flixel.org" target="_blank">Flixel游戏引擎</a>，它把四叉树隐藏在碰撞检测的API里，用户不需要直接操作四叉树。
调用碰撞检测的同时，引擎内部就自动建树了。大家都看到Flixel引擎的效率是非常高的，各种大小相差几倍的精灵放在一起检测也没有问题，这也是四叉树分区相比网格分区的一个优势。可惜Flixel里的四叉树
经过了各种优化，已经不是传统四叉树的样子了，很难分离出来。as3ds包里也没有四叉树的实现，所以我又google了一下，在网上看到一个写的比较清晰的JavaScript版本<a href="http://www.mikechambers.com/blog/2011/03/21/javascript-quadtree-implementation/" target="_blank">JavaScript QuadTree Implementation</a>
又无意中搜到<a href="http://code.google.com/p/ghostcat/source/browse/trunk/GhostCat/src/ghostcat/algorithm/QuadTree.as?r=584" target="_blank">GhostCat里也有一个四叉树类</a>，GhostCat的这个版本是我看到写的最简洁的四叉树，很是佩服作者。
不过经过研究，他们有一个共同的问题就是只能把点(point)添加到树中，这对子弹或者小球的碰撞检测还好，但通常游戏中不论是MC还是Bitmap大都是有宽高的方形。如果把他们按照点来分配进树的话，就会有漏检查的情况出现。
下图是我将上边提到的，mikechambers写的JavaScript版本转成AS3后发现的问题，mike虽然已经将压线的矩形作为有可能碰撞的对象返回，但百密一疏，由于他是按照点来分配的，始终会有漏检</p>
<p><img src="/image/tree02.jpg" alt="/image/tree02.jpg"></p>
<p>所以我又回头看Flixel里的四叉树是怎么插入的，最后综合以上所有版本，我写了我的这个四叉树类。就是上边演示的那个，代码在文章末尾下载包里，太长就不贴了，我记得应该是写了很多注释。大家可以研究讨论一下。</p>
<h3>图</h3>
<p>图由节点node和指针arc组成，图的遍历与树差不多，分为广度优先遍历(与树的后序遍历)，和深度优先遍历（与树的前序遍历)。</p>
<p>图在游戏中通常用作保存地图，网上流行的页游地图都是tilemap的，区块其实就是简化了指针之后的图，所以他们的理论是相通的。</p>
<p>请看基于图的A*算法演示：</p>
<p>因为我用了Adobe kuler配了一下色，所以漂亮了许多 ：）</p>
<script type="text/javascript">swfobject.embedSWF("/image/GraphTest.swf","/image/GraphTest","700","700","9.0.0","/image/expressInstall.swf")</script><p><div id="/image/GraphTest"><br /><p><a href="http://www.adobe.com/go/getflashplayer"><img src="http://www.adobe.com/images/shared/download_buttons/get_flash_player.gif" alt="Get Adobe Flash player" /></a></p></p></div>


<p>为什么要用A*寻路而不是广度或深度遍历寻路呢？因为他们本身是很傻很执着的，由于不知道终点位置而盲目扫描整个图，这会浪费效能，而A星之所以叫做启发式寻路，是因为它事先预测了终点的大概方向而向那个方向扫描，这样增加了效率。</p>
<p>我是照着这篇很有名的A Star入门教程写的代码：<a href="http://www.vckbase.com/document/viewdoc/?id=1422" target="_blank">A*寻路初探</a></p>
<p>因为他是基于区块的，所以要改动一下，而需要改变的只有一个地方，他的文章里是循环检查 <strong>周围相邻的8格</strong> ，而我们改成  <strong>连通的所有节点</strong>  就ok了。</p>
<p>我的Astar类已经按照教程逐句做了注释，对照教程应该很容易理解了。</p>
<p>与教程不同的地方就是：</p>
<p>1) 我使用了上边提到Heap优先队列来优化了open列表，使F值最小的始终在列表最上方，这个在教程后边也有讲。</p>
<p>2）改用了 <strong>距离的启发值函数</strong> 计算H值，而原教程使用的 <em>曼哈顿距离启发值函数</em> 也有提供，可以替换，只是貌似没有距离的效果好,大家可以自己试一下。</p>
<pre><code><figure class="highlight"><pre>package examples<span class="variable">.Astar</span>
{
    import ds<span class="variable">.Graph</span>;
    import ds<span class="variable">.GraphArc</span>;
    import ds<span class="variable">.GraphNode</span>;
    import ds<span class="variable">.Heap</span>;

    <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="id">Astar</span></span>
    {            
    <span class="comment">/**
     *   距离启发函数
     */</span>
    <span class="keyword">public</span> <span class="keyword">static</span> function distance(startNode:GraphNode,endNode:GraphNode):Number
    {
     <span class="keyword">return</span>  Math<span class="variable">.sqrt</span>( (endNode<span class="variable">.data</span><span class="variable">.x</span> - startNode<span class="variable">.data</span><span class="variable">.x</span>)*(endNode<span class="variable">.data</span><span class="variable">.x</span> - startNode<span class="variable">.data</span><span class="variable">.x</span>) + (endNode<span class="variable">.data</span><span class="variable">.y</span> - startNode<span class="variable">.data</span><span class="variable">.y</span>)*(endNode<span class="variable">.data</span><span class="variable">.y</span> - startNode<span class="variable">.data</span><span class="variable">.y</span>))
    }
    <span class="comment">/**
     *  曼哈顿启发函数
     */</span>
    <span class="keyword">public</span> <span class="keyword">static</span> function manhattan(startNode:GraphNode,endNode:GraphNode):Number
    {
    <span class="keyword">return</span>  Math<span class="variable">.abs</span>(endNode<span class="variable">.data</span><span class="variable">.x</span> - startNode<span class="variable">.data</span><span class="variable">.x</span>)+Math<span class="variable">.abs</span>(endNode<span class="variable">.data</span><span class="variable">.y</span> - endNode<span class="variable">.data</span><span class="variable">.y</span>);
    }
    <span class="comment">/**
     *  寻路
     */</span>
    <span class="keyword">public</span> <span class="keyword">static</span> function find(graph:Graph , startIndex:<span class="keyword">int</span>,endIndex:<span class="keyword">int</span>):Array
    {
    <span class="comment">//还原值</span>
    <span class="keyword">for</span> each (var n:AstarNode in graph<span class="variable">.nodes</span>)
    {
        n<span class="variable">.f</span> = n<span class="variable">.g</span> = n<span class="variable">.f</span> = <span class="number">0</span>
        n<span class="variable">.parent</span> = null

    }
    <span class="comment">//根据f值升序排列 </span>
    var open:Heap = new Heap(function(a:AstarNode,b:AstarNode):Number{<span class="keyword">return</span> b<span class="variable">.f</span> - a<span class="variable">.f</span>}); 

    var close:Array = []
    <span class="comment">//1，把起始节点添加到开启列表。</span>
    AstarNode(graph<span class="variable">.getNode</span>(startIndex))<span class="variable">.f</span> =<span class="number">0</span>    
    open<span class="variable">.enqueue</span>(graph<span class="variable">.getNode</span>(startIndex));
    <span class="comment">//2，重复如下的工作：</span>
    <span class="keyword">while</span>(open<span class="variable">.heap</span><span class="variable">.length</span>&gt;<span class="number">0</span>)
    {
        <span class="comment">//a) 寻找开启列表中F值最低的节点。我们称它为当前节点。 </span>
        var cur:AstarNode = AstarNode(open<span class="variable">.dequeue</span>())
        <span class="keyword">if</span>(cur==graph<span class="variable">.getNode</span>(endIndex))<span class="keyword">return</span>  Astar<span class="variable">.getPath</span>(cur) 
               <span class="comment">//把目标格添加进了关闭列表，这时候路径被找到    </span>

        <span class="comment">//b) 把它切换到关闭列表。</span>
        close<span class="variable">.push</span>(cur)
        <span class="comment">//c) 对相邻的格中的每一个？</span>
        <span class="keyword">for</span> each(var i:GraphArc in cur<span class="variable">.arcs</span>)
        {
            var test:AstarNode = AstarNode(i<span class="variable">.targetNode</span>)
            <span class="comment">//如果它不可通过或者已经在关闭列表中，略过它。</span>
            <span class="keyword">if</span>(close<span class="variable">.indexOf</span>(test)&gt;=<span class="number">0</span>)<span class="keyword">continue</span>
            <span class="comment">//如果它不在开启列表中，把它添加进去。</span>
                        <span class="comment">//把当前格作为这一格的父节点。记录这一格的F,G,和H值。</span>

            <span class="keyword">if</span>(open<span class="variable">.heap</span><span class="variable">.indexOf</span>(test)&lt;<span class="number">0</span>)
            {                        
                test<span class="variable">.parent</span> =cur
                test<span class="variable">.g</span> = cur<span class="variable">.getArc</span>(test)<span class="variable">.weight</span>
                test<span class="variable">.h</span> = Astar<span class="variable">.distance</span>(test,graph<span class="variable">.getNode</span>(endIndex))
                <span class="comment">// 这里使用距离启发值，原教程使用的曼哈顿启发函数</span>
                                <span class="comment">//可以替换成这句  Astar.manhattan(test,graph.getNode(endIndex))</span>
                test<span class="variable">.f</span> = test<span class="variable">.g</span> + test<span class="variable">.h</span>
                open<span class="variable">.enqueue</span>(test);
            }<span class="keyword">else</span>
            {
             <span class="comment">//如果它已经在开启列表中，用G值为参考检查新的路径是否更好。</span>
                         <span class="comment">//更低的G值意味着更好的路径。</span>
             <span class="comment">//如果是这样，就把这一格的父节点改成当前格，并且重新计算这一格的G和F值。</span>
                         <span class="comment">//如果你保持你的开启列表按F值排序,改变之后你可能需要重新对开启列表排序。</span>
                <span class="keyword">if</span>( cur<span class="variable">.g</span>+ cur<span class="variable">.getArc</span>(test)<span class="variable">.weight</span> &lt; test<span class="variable">.g</span>)
                {
                    test<span class="variable">.parent</span> = cur
                    test<span class="variable">.g</span> = cur<span class="variable">.g</span>+ cur<span class="variable">.getArc</span>(test)<span class="variable">.weight</span>
                    test<span class="variable">.f</span> = test<span class="variable">.h</span>+test<span class="variable">.g</span>
                    open<span class="variable">.modify</span>(test,test);
                }                        
            }

         }    
    }
    <span class="keyword">return</span> []
        }    
    <span class="keyword">public</span> <span class="keyword">static</span> function getPath(node:AstarNode):Array
    {
        var arr:Array = [node]
        <span class="keyword">while</span>(node<span class="variable">.parent</span>)
        {
            arr<span class="variable">.push</span>(node<span class="variable">.parent</span>)
            node = node<span class="variable">.parent</span>
        }        
        <span class="keyword">return</span> arr
    }

    }
}
</pre></figure></code></pre>
<p>以上就是我目前了解到的对于flash开发还比较实用的数据结构了，欢迎补充，以供我继续学习。</p>
<p><a href="/image/ds1_0.zip" target="_blank">所有完整代码在此下载 </a></p>
<p>我的版本很多方法都是直接抄自<a href="http://lab.polygonal.de/as3ds/" target="_blank">as3ds</a> ，有些方法却改成比较容易理解的方式，提供的方法也少很多，仅供学习参考.</p>
]]></content>
    <category scheme="http://nshen.net/categories/article/" term="article"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[笔记《版本控制之道--使用Git》]]></title>
    <link href="http://nshen.net/article/2011-08-03/git-note/"/>
    <id>http://nshen.net/article/2011-08-03/git-note/</id>
    <published>2011-08-03T05:30:20.000Z</published>
    <updated>2013-06-24T11:16:49.000Z</updated>
    <content type="html"><![CDATA[<p>我认为每个学过Git的人都应该做过类似这种笔记，因为Git命令太多看着看着就把前边看过的忘了，之前我也看过Git，但是一直没用，现在一看几乎没有印象了，所以这次我要把我看到的命令记下来给我自己备忘。</p>
<a name="more"></a> 

<p>Git已经是最流行的版本控制系统了，网上相关的免费学习资源很多，我见过的中文书籍就有：</p>
<ul>
<li><a href="http://gitbook.liuhui998.com/index.html" target="_blank">Git Community Book 中文版</a></li>
<li><a href="http://progit.org/book/zh/" target="_blank">Pro Git 中文版 </a></li>
<li><a href="http://www-cs-students.stanford.edu/~blynn/gitmagic/intl/zh_cn/" target="_blank">Git Magic 中文版 </a></li>
</ul>
<p>但我是买的一本纸质书叫做《版本控制之道--使用Git》，下边是我记录的几乎是整本书讲过的所有命令：</p>
<hr>
<h3>设置</h3>
<figure class="highlight lang-dos"><pre>
git config --global user.name "Nshen" //必须
git config --global user.email "nshen121@gmail.com"//必须
git config --global color.ui "always"
//或者"auto", always不仅Base环境是彩色，Dos里也是彩色的。

git config --global core.editor notepad.exe//设为windows记事本
git config --global alias.ci "commit"//别名缩写
git config --global merge.tool//可以设置合并工具
git config --global --list//查看设置

</pre></figure>


<p>其实最后这些设置都保存在</p>
<figure class="highlight lang-dos"><pre>
C:\Documents and Settings\用户名\.gitconfig 文件下（windows）
</pre></figure>


<hr>
<p><strong>查看帮助：</strong> </p>
<figure class="highlight lang-dos"><pre>git help command
</pre></figure>


<p><strong>初始化 :</strong></p>
<figure class="highlight lang-dos"><pre>git init
</pre></figure>


<p><strong>纳入版本控制:</strong></p>
<figure class="highlight lang-dos"><pre>
git add *.txt   //添加所有txt文件
git add README  //添加单个文件
git add .        //添加所有文件包括子目录，但不包括空目录
</pre></figure>


<p>add命令是个多功能命令，根据目标文件的状态不同，此命令的效果也不同：可以用它开始跟踪新文件，或者把已跟踪的文件放到暂存区，还能用于合并时把有冲突的文件标记为已解决状态等）注意每次修改后都要重新add,不然就会提交之前add时的版本。</p>
<figure class="highlight lang-dos"><pre>git add -i  //进入交互式add
git add -p //直接进入补丁模式，可以暂存修改的一部分。
</pre></figure>


<p><strong>提交：</strong></p>
<figure class="highlight lang-dos"><pre>git commit -m "initial project version"
git commit -m "something" someFile  //提交指定文件
git commit -C HEAD -a --amend  //复用HEAD留言，增补提交（修改小错误，而不增加提交记录，掩盖自己的小马虎）
</pre></figure>



<p><strong>参数:</strong></p>
<figure class="highlight lang-dos"><pre>
-m "提交的说明"
-a 动把所有已经跟踪过的文件暂存,并提交.(工作目录中修改过的文件都提交到版本库，不需一个一个手动add了)
--amend 增补提交
-C 复用指定提交的提交留言
-c 打开编辑器在已有的提交基础上编辑修改
</pre></figure>


<p>e.g 修改最后一次提交:</p>
<figure class="highlight lang-dos"><pre>
git commit -m 'initial commit'
git add forgotten_file
git commit --amend
</pre></figure>


<p>如果没有修改就相当于更改提交说明,上边3个命令得到一个提交.</p>
<hr>
<p><strong>忽略提交的文件：</strong></p>
<p>所有人都需要忽略的文件要写在<strong>.gitignore</strong>文件里，而只有自己的个人偏好需要忽略的文件要写在.git/info/exclude文件中</p>
<p><strong>语法：</strong></p>
<figure class="highlight lang-dos"><pre>
# 此为注释 – 将被 Git 忽略
*.a       # 忽略所有 .a 结尾的文件
!lib.a    # 但 lib.a 除外
*.[oa]    #忽略以.o或.a结尾的文件
*~        #忽略以~结尾的文件
/TODO     # 仅仅忽略项目根目录下的 TODO 文件，不包括 subdir/TODO
build/    # 忽略 build/ 目录下的所有文件
doc/*.txt # 会忽略 doc/notes.txt 但不包括 doc/server/arch.txt
</pre></figure>


<h3>查看文件改动：</h3>
<figure class="highlight lang-dos"><pre>git diff   // 比较工作目录与缓存区的区别
git diff --cached 或者 git diff --staged  //缓存区与版本库里的区别
git diff HEAD //三者的区别
</pre></figure>


<p>请注意，单单 git diff 不过是显示还没有暂存起来的改动，而不是这次工作和上次提交之间的差异。
所以有时候你一下子暂存了所有更新过的文件后，运行 git diff 后却什么也没有，就是这个原因。</p>
<figure class="highlight lang-dos"><pre>git diff <span class="number">18</span>f822e 	   //<span class="number">18</span>f822e这个版本与当前目录的区别
git diff aaaaa..bbbbb //比较aaaaa与bbbbb之间差别
git diff --stat 		  //可以统计数据，比较特别的命令
</pre></figure>


<h3>重命名，移动，删除文件：</h3>
<figure class="highlight lang-dos"><pre>git mv file_from file_to  //改名或移动

$ git mv README.txt README
$ git status
# On branch master
# Your branch is ahead of 'origin/master' by <span class="number">1</span> commit.
#
# Changes to be committed:
#   (use "git reset HEAD &lt;file&gt;..." to unstage)
#
#       renamed:    README.txt -&gt; README
</pre></figure>


<p>其实，运行 git mv 就相当于运行了下面三条命令：</p>
<figure class="highlight lang-dos"><pre>$ mv README.txt README
$ git rm README.txt
$ git add README
</pre></figure>


<p>必须调用 &quot;git rm 文件名&quot;  ,从暂存区移除,并且文件也被删除</p>
<p>如果只是手工删除了文件,运行git status时会出现</p>
<figure class="highlight lang-dos"><pre># Changed but <span class="flow">not</span> updated:
#   (use "git add/rm &lt;file&gt;..." to update what will be committed)
#
#       deleted:    grit.gemspec
</pre></figure>


<p>此时必须再运行 &quot;git rm 文件名&quot;,才会在提交时候不再纳入版本管理.
如果删除之前修改过并且已经add到缓存区了的话,则必须强制删除 -f</p>
<p>另外一种情况是，我们想把文件从Git仓库中删除（亦即从暂存区域移除），但仍然希望保留在当前工作目录中。换句话说，仅是从跟踪清单中删除。比如一些大型日志文件或者一堆.a编译文件，不小心纳入仓库后，要移除跟踪但不删除文件，以便稍后在 .gitignore 文件中补上，用 --cached 选项即可。</p>
<hr>
<h3>查看状态：</h3>
<h4>查看当前状态:</h4>
<p><strong>git status</strong></p>
<figure class="highlight lang-dos"><pre>$ git status
# On branch master
# Changes to be committed:  //只要在这行后边的,说明放入暂存区了
#   (use "git reset HEAD &lt;file&gt;..." to unstage) //想取消放入缓存 git reset HEAD README
#
#     new file:   README
# Changed but <span class="flow">not</span> updated:  //跟踪文件内容改变,但还没有放到暂存区,需要git add 命令才会放到暂存区
#   (use "git add &lt;file&gt;..." to update what will be committed)
#   (use "git checkout -- &lt;file&gt;..." to discard changes <span class="flow">in</span> working directory)
#    //删除修改,恢复到之前版本,有危险 (如果想保留并且回退版本用stashing 和分支来处理)
#     modified:   benchmarks.rb
</pre></figure>


<hr>
<h4>查看提交历史:</h4>
<p><strong>git log</strong></p>
<p>这时“j”向下浏览，“k”向上浏览，“q”退出</p>
<figure class="highlight lang-dos"><pre>git log --pretty=oneline    //一行显示
        --pretty="%h %s"   //以各种格式输出

git log –p -<span class="number">2</span>    //-p显示每次提交的内容差异  -<span class="number">2</span>表示最近<span class="number">2</span>次更改

	git log --since "<span class="number">5</span> hours"
	 		--since "<span class="number">3</span> hours"
			--since "<span class="number">1</span> minute"
	 		--before ="<span class="number">2008</span>-<span class="number">10</span>.<span class="number">01</span>"

git log <span class="number">27</span>j34j3j..<span class="number">03</span>u43u23  //最老版本..最新版本（不包括起点只包括终点）

git log <span class="number">34</span>j4j4..HEAD
git log fhfs8fh..           //省略HEAD

git log "HEAD^^".."HEAD^"     //windows必须加引号表示回溯上一个提交

git log -<span class="number">1</span> HEAD~<span class="number">1</span>  //相当于git log -<span class="number">1</span> HEAD^

git log --graph //图形化显示分支

git log --graph --pretty=oneline //一行图形化显示分支
</pre></figure>



<h3>问责：查明谁修改了代码</h3>
<figure class="highlight lang-dos"><pre>git blame hello.html //你也可以用"-L"参数在命令(blame)中指定开始和结束行:
git blame -L <span class="number">12</span>,+<span class="number">10</span> hello.html  //<span class="number">12</span>到<span class="number">22</span>行
blame还可以跟踪内容复制，文件复制，略，见版本控制之道 <span class="number">79</span>页
</pre></figure>


<h3>撤销：</h3>
<h4>撤销缓存区的修改（没有commit的）</h4>
<figure class="highlight lang-dos"><pre>git checkout head 文件名 //撤销暂存区的修改  
git checkout head readme.txt todo.txt
git checkout head *.txt
git checkout head .  //撤销所有
</pre></figure>

<h4>反转提交：</h4>
<figure class="highlight lang-dos"><pre>git revert HEAD  //创建一个反向的新提交抵消原来的提交改动
如果需要反转多个，必须从最后的开始反转, 加 -n可以不马上提交，之后一起提交。
git revert -n HEAD
git revert -n <span class="number">54</span>efhds
git commit -m "revert head and <span class="number">54</span>efhds"
</pre></figure>


<h4>复位：还没有commit，让工作目录回到上次提交时的状态</h4>
<figure class="highlight lang-dos"><pre>git reset --hard HEAD   //所有未提交的内容清空，这会让"git diff" 和"git diff --cached"命令的显示法都变为空
git reset --soft HEAD  //复位版本库，暂存差异，便于提交中发现错误需要更改时有用（例如私人密码放到里边了）
</pre></figure>


<h3>分支：</h3>
<h4>在当前分支末梢建立分支：</h4>
<figure class="highlight lang-dos"><pre>git branch RB_1.<span class="number">0</span>（建立分支不会自动切换过去）
</pre></figure>


<h4>切换分支：</h4>
<figure class="highlight lang-dos"><pre>git checkout RB_1.<span class="number">0</span>（切换到RB_1.<span class="number">0</span>分支）
</pre></figure>


<h4>创建并切换分支：</h4>
<figure class="highlight lang-dos"><pre>git checkout -b RB_1.<span class="number">0</span>（简化上边<span class="number">2</span>步操作）
</pre></figure>


<h4>删除分支：</h4>
<figure class="highlight lang-dos"><pre>git branch -d RB_1.<span class="number">0</span>
</pre></figure>


<h4>基于某次提交、分支或标签创建新分支:</h4>
<figure class="highlight lang-dos"><pre>git branch RB_1.<span class="number">0</span> master
git branch RB_1.<span class="number">0</span> <span class="number">6</span>fe57de0
git branch Rb_1.<span class="number">01</span> <span class="number">1</span>.<span class="number">0</span>
</pre></figure>


<h4>查看分支：</h4>
<figure class="highlight lang-dos"><pre>git branch //列出本地分支
  iss53
* master  //*号表示当前所在分支
  testing


git branch -r //显示远程分支
git branch -a //列出所有分支
</pre></figure>


<h4>分支重命名：</h4>
<figure class="highlight lang-dos"><pre>git branch -m master mymaster	
		   -M 大写M会覆盖同名的分支
</pre></figure>


<h4>合并分支：</h4>
<p>直接合并：</p>
<figure class="highlight lang-dos"><pre>git merge 想合并到当前分支的源分支名
git merge --no-commit 分支 //合并但不提交 
</pre></figure>


<p>压合合并：将分支压合成一条commit记录，并合并过来</p>
<figure class="highlight lang-dos"><pre>git merge --squash 某bug分支
git commit -m "修复某bug"
</pre></figure>


<p>拣选合并：只合并一个提交</p>
<figure class="highlight lang-dos"><pre>git cherry-pick <span class="number">321</span>d76f
</pre></figure>



<p>如果需要连续拣选，就需要加 -n参数
然后再git commit ，但不要加-m参数，编辑器就会使用刚拣选的提交留言作为现在的留言。</p>
<h3>标签Tag:</h3>
<h4>查看标签:</h4>
<figure class="highlight lang-dos"><pre>git tag
</pre></figure>


<h4>创建标签：</h4>
<figure class="highlight lang-dos"><pre>git tag <span class="number">1</span>.<span class="number">0</span>  //在当前分支最后一次提交创建标签
git tag <span class="number">1</span>.<span class="number">0</span> RB_1.<span class="number">0</span> //基于RB_1.<span class="number">0</span>分支的最新踢脚创建标签
git tag <span class="number">1</span>.<span class="number">0</span> ae468d8kt //为某次提交创建标签
</pre></figure>


<h4>检出标签：</h4>
<figure class="highlight lang-dos"><pre>git checkout <span class="number">1</span>.<span class="number">0</span> //检出标签与检出分支一样操作，但检出标签后用git branch查看本地分支会发现你现在不再任何分支上
这时你不应该修改，而应该立即基于此标签创建一个分支
git checkout -b from-<span class="number">1</span>.<span class="number">0</span>
</pre></figure>


<h3>变基：</h3>
<figure class="highlight lang-dos"><pre><span class="number">1</span>. git rebase RB_1.<span class="number">01</span> //也许修改过一个bug，希望新版本变基到RB_1.<span class="number">01</span>分支上
<span class="number">2</span>. 手动解决冲突         //如果解决不了直接git rebase --skip或--abort来跳过特定提交或完全放弃变基
<span class="number">3</span>. git add xxx.html //冲突解决
<span class="number">4</span>. git rebase --continue


git rebase --onto HEAD^^ HEAD^ HEAD 


//--onto参数可以改写历史抹掉中间的参数，将倒数第一个参数变基到倒数第<span class="number">3</span>个参数，为防止出错建议在试验性分支上先试验。

rebase -i 可以排序历史记录，多个提交合并为<span class="number">1</span>个，一个提交分解成多个提交 ，
详见版本控制之道p86 ，需要编辑器支持，windows记事本不行
</pre></figure>


<h3>远程相关：</h3>
<figure class="highlight lang-dos"><pre>git clone git://github.com/schacon/grit.git   //从现有仓库克隆
git clone git://github.com/schacon/grit.git mygrit //换名,唯一区别就是新建的目录成了mygrit,其他都一样
</pre></figure>


<h4>添加远程仓库:</h4>
<figure class="highlight lang-dos"><pre>
git <span class="comment">remote add pb git://github.com/paulboone/ticgit.git</span>
clone会默认添加origin仓库，如果原本用git init创建的版本库，后来又想提交到远程版本库，就可以用下边的办法
git <span class="comment">remote add origin git@example.com:/xxxxxx</span>

</pre></figure>


<h4>查看远程分支：</h4>
<figure class="highlight lang-dos"><pre>git <span class="comment">remote  -v //查看远程仓库,默认clone后,应该有一个origin仓库,-v显示对应的clone地址</span>
git <span class="comment">remote show origin //查看远程仓库信息</span>
</pre></figure>


<h4>远程仓库重命名和删除:</h4>
<figure class="highlight lang-dos"><pre>git <span class="comment">remote rename pb paul</span>
git <span class="comment">remote rm paul</span>
</pre></figure>


<h4>获取数据：</h4>
<figure class="highlight lang-dos"><pre>git fetch [<span class="comment">remote-name] 拉取远程仓库到本地远程仓库,不自动合并    //$ git fetch origin</span>
$ git fetch pb
<span class="comment">remote: Counting objects: 58, done.</span>
<span class="comment">remote: Compressing objects: 100% (41/41), done.</span>
<span class="comment">remote: Total 44 (delta 24), reused 1 (delta 0)</span>
Unpacking objects: <span class="number">100</span>% (<span class="number">44</span>/<span class="number">44</span>), done.
From git://github.com/paulboone/ticgit
* [new branch]      master     -&gt; pb/master
* [new branch]      ticgit     -&gt; pb/ticgit
</pre></figure>


<p>现在pb/master可以在本地访问了,你可以合并到自己的某个分支,或者切换到这个分支看看有什么有趣的更新</p>
<p>git pull 抓取数据合并到工作目录中当前分支</p>
<h4>推送数据：</h4>
<figure class="highlight lang-dos"><pre>git push [<span class="comment">remote-name] [branch-name]  //默认为 git push origin master</span>

git push origin serverfix   //推送分支，其实是下边一句的简化,提取我的 serverfix 并更新到远程仓库的 serverfix

git push origin serverfix:serferfix

git push origin :serverfix //这个语法用于删除,只要把分号前留空
</pre></figure>


<h3>其他：</h3>
<figure class="highlight lang-dos"><pre>git gc  //垃圾回收，每隔一段时间例如一个月运行一次可以减少磁盘占用空间。
git reflog //最后的保障,列出误删的东东
git bisect //二分查找，版本控制之道p124页，略
</pre></figure>


<h4>归档版本库，导出压缩包:</h4>
<figure class="highlight lang-dos"><pre>git archive --format=格式 --prefix=目录/ 版本&gt;压缩包.zip
git archive --format=zip head&gt;test.zip
git archive --format=tar --prefix=mysite-<span class="number">1</span>.<span class="number">0</span>/ <span class="number">1</span>.<span class="number">0</span> | gzip&gt;mysite-<span class="number">1</span>.<span class="number">0</span>.tar.gz
git archive --format=zip --prefix=mysite-<span class="number">1</span>.<span class="number">0</span>/ <span class="number">1</span>.<span class="number">0</span> &gt;mysie-<span class="number">1</span>.<span class="number">0</span>.zip
</pre></figure>
]]></content>
    <category scheme="http://nshen.net/categories/article/" term="article"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[跨IOS，Android的小游戏《TinyKingdom》]]></title>
    <link href="http://nshen.net/project/2011-07-05/tiny-kingdom/"/>
    <id>http://nshen.net/project/2011-07-05/tiny-kingdom/</id>
    <published>2011-07-05T06:56:31.000Z</published>
    <updated>2013-06-22T01:49:03.000Z</updated>
    <content type="html"><![CDATA[<p>目前在iphone4和Android的g7上运行效果差不多，具体见视频。</p>
<a name="more"></a>

<p>对游戏机制设计不是很满意，做了10多关，现暂停开发中。</p>
<h3>2011-07-05在G7上测试视频：</h3>
<embed src="http://www.tudou.com/v/WYv-ljAL1xs/&rpid=9086081/v.swf" type="application/x-shockwave-flash" allowscriptaccess="always" allowfullscreen="true" wmode="opaque" width="480" height="400"></embed>]]></content>
    <category scheme="http://nshen.net/tags/游戏/" term="游戏"/>
    <category scheme="http://nshen.net/categories/project/" term="project"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[PDF2MAG：一键pdf文档转flash电子杂志]]></title>
    <link href="http://nshen.net/project/2011-01-12/pdf2mag/"/>
    <id>http://nshen.net/project/2011-01-12/pdf2mag/</id>
    <published>2011-01-12T06:56:31.000Z</published>
    <updated>2013-06-22T01:32:22.000Z</updated>
    <content type="html"><![CDATA[<p>PDF2MAG 是我基于Adobe Air编写的一款将pdf文档快速转为flash电子杂志的软件。
新版本又增加了支持转换为FlexPaper文档。</p>
<a name="more"></a>

<p>PDF2MAG 可以快速将PDF文档转换为适合在网页中浏览的Flash电子杂志.</p>
<h4>演示:</h4>
<ul>
<li><a href="http://riabook.cn/files/pdf2mag/demo/">硬质封面演示</a></li>
<li><a href="http://riabook.cn/files/pdf2mag/demo13/">软质封面演示</a></li>
<li><a href="http://riabook.cn/files/pdf2mag/flexpaper/">Flexpaper文档演示</a></li>
</ul>
<hr>
<h3>下载地址：</h3>
<p><a href="http://u.115.com/file/c4zvhrqq#DownloadPDF2Mag_1_3.rar">PDF2MAG 1.3下载（115网盘）</a></p>
<p>注意：此软件<strong>个人网站</strong>与<strong>商业网站</strong>均完全<strong>免费</strong>使用，但请发邮件通知我一下网址。 nshen121[at]gmail.com
好让我评估是否投入精力继续开发此软件。</p>
<hr>
<h3>使用方法：</h3>
<ol>
<li>解压后点击exe文件进行安装,如安装失败请先 点此安装Adobe Air运行环境。 </li>
<li>安装成功后，会发现这个图标。 </li>
</ol>
<p><img src="/image/pdf2mag_01.png" alt=""></p>
<ol>
<li>点击打开，选择生成杂志或是FlexPaper文档，并选取pdf文件和一个空文件夹作为输出目录。 </li>
</ol>
<p><img src="/image/pdf2mag02.jpg" alt=""></p>
<ol>
<li>转换成功后，将输出目录中的文件通过ftp上传到服务器空间中即可。</li>
</ol>
<hr>
<h3>一起发展此软件？</h3>
<p>个人精力能力有限,2011年开始停止更新了，如仍有兴趣一起发展此软件的话联系我 nshen121[at]gmail.com</p>
<hr>
<h3>常见问题：</h3>
<h5>问:转换后报错[RPC Fault faultString=&quot;Error #2148&quot; faultCode=&quot;InvokeFailed&quot; faultDetail=&quot;null&quot;]</h5>
<p>答:这是FlashPlayer的本地安全限制,使用FTP上传到服务器应该就好了:)</p>
<h5>问:生成的杂志目录不正确，可以修改吗？</h5>
<p>答:所有的配置信息都在生成的目录下config.xml里,用记事本打开,依照下边格式可以编辑目录,然后保存就可以了.</p>
<p><figure class="highlight lang-xml"><pre><span class="tag">&lt;<span class="title">contents</span>&gt;</span> 
 <span class="tag">&lt;<span class="title">node</span> <span class="attribute">label</span>=<span class="value">"章："</span> <span class="attribute">page</span>=<span class="value">"1"</span>&gt;</span> 
   <span class="tag">&lt;<span class="title">node</span> <span class="attribute">label</span>=<span class="value">"节1"</span> <span class="attribute">page</span>=<span class="value">"1"</span>/&gt;</span> 
   <span class="tag">&lt;<span class="title">node</span> <span class="attribute">label</span>=<span class="value">"节2"</span> <span class="attribute">page</span>=<span class="value">"2"</span>/&gt;</span> 
   <span class="tag">&lt;<span class="title">node</span> <span class="attribute">label</span>=<span class="value">"节3"</span> <span class="attribute">page</span>=<span class="value">"3"</span>/&gt;</span> 
 <span class="tag">&lt;/<span class="title">node</span>&gt;</span> <span class="tag">&lt;<span class="title">node</span> <span class="attribute">label</span>=<span class="value">"章2："</span> <span class="attribute">page</span>=<span class="value">"1"</span>&gt;</span>
   <span class="tag">&lt;<span class="title">node</span> <span class="attribute">label</span>=<span class="value">"节1"</span> <span class="attribute">page</span>=<span class="value">"1"</span>/&gt;</span> 
 <span class="tag">&lt;/<span class="title">node</span>&gt;</span> 
<span class="tag">&lt;/<span class="title">contents</span>&gt;</span>
</pre></figure></p>
<h5>问:转换FlexPaper后没有显示内容,最后几行输出 Error This file is too complex to renderer.？</h5>
<p>答:目前发现pdf2swf转换复杂的pdf会有这个问题,建议尝试优化一下pdf再试。暂时还无法解决。</p>
<hr>
<h3>版本历史：</h3>
<ul>
<li><p><strong>1.30: 2011-01-12</strong>
增加生成FlexPaper1.4.1功能(FlexPaper详见: <a href="http://flexpaper.devaldi.com/">http://flexpaper.devaldi.com/</a>)</p>
</li>
<li><p><strong>1.21: 2010-09-2</strong>
更新为Adobe AIR 2正式版</p>
</li>
<li><p><strong>1.20: 2010-02-10</strong>
基于Adobe Air 2 beta 2 loading图缩小 页数显示去掉cover backover,使生成后的页数与原pdf页数显示一致,方便添加目录 解决pdf总页数是奇数时翻到最后一页报错的bug 使用位图优化,翻页速度大幅提升 右键菜单增加版本信息 增加生成杂志默认显示大小的设置 增加软硬质封面设置 放大缩小可用 目录功能可用 右上角植入个小图标打印链接</p>
</li>
<li><p><strong>1.10: 2010-01-25</strong>
在1.0基础上添加了转换过程中的文字提示 基于Adobe Air 2 beta 1</p>
</li>
<li><p><strong>1.00: 2010-01-25</strong>
最初版本,实现基本功能 基于Adobe Air 2 beta 1</p>
</li>
</ul>
]]></content>
    <category scheme="http://nshen.net/tags/pdf2mag/" term="pdf2mag"/>
    <category scheme="http://nshen.net/categories/project/" term="project"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[《The Little Bee》独立小游戏第一次尝试]]></title>
    <link href="http://nshen.net/project/2010-08-13/the-little-bee/"/>
    <id>http://nshen.net/project/2010-08-13/the-little-bee/</id>
    <published>2010-08-13T06:56:31.000Z</published>
    <updated>2013-06-22T02:05:44.000Z</updated>
    <content type="html"><![CDATA[<p>《这只小蜜蜂》1代，程序策划美术全包，原创玩法，具有躲避和升级策略元素的纵版飞行射击游戏。后台统计各平台获10万+玩家流量。</p>
<a name="more"></a>

<h3>游戏地址：</h3>
   <p>
<a href="http://www.4399.com/flash/37351_3.htm" target="_blank">[ 中文版 ]</a>   <a href="http://www.mochimedia.com/games/play/the-little-bee/" target="_blank">[English version]</a>   </p>

<p>这是个加入躲避和武器升级元素的飞行射击游戏，你是一只小蜜蜂，升级武器，消灭可恶的Boss，并挑战高分吧！！！</p>
<h3>封面图片：</h3>
<p><img src="/image/tlb01.png" alt="the little bee"></br></br></p>
<h3>2代策划中</h3>
<p><img src="/image/tlb02.png" alt="the little bee 2"></p>
]]></content>
    <category scheme="http://nshen.net/tags/游戏/" term="游戏"/>
    <category scheme="http://nshen.net/categories/project/" term="project"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[《ActionScript 3.0: Visual QuickStart Guide》翻译]]></title>
    <link href="http://nshen.net/project/2009-11-19/as3-visual-quickstart-guide/"/>
    <id>http://nshen.net/project/2009-11-19/as3-visual-quickstart-guide/</id>
    <published>2009-11-19T09:19:44.000Z</published>
    <updated>2013-07-14T05:43:54.000Z</updated>
    <content type="html"><![CDATA[<p>我跟达达翻译的书终于出版了。</p>
<a name="more"></a>

<p><img src="/image/as3JiChuJiaoCheng_1.jpg" alt="ActionScript 3.0基础教程"></br></p>
<hr>
<h3>出版社寄来的样书，已经送给曾经关注的朋友们了：</h3>
<p><img src="/image/as3JiChuJiaoCheng_2.jpg"></br></p>
<p><img src="/image/as3JiChuJiaoCheng_3.jpg"></br></p>
<hr>
<h3>后记：</h3>
<p>众所周知本人只有高中学历，英语也就在初中水平，由于这本书英文和内容都非常简单，为了练习英语就主动跟出版社申请翻译了，记得在北京瑞德上班时，每天下班后回到出租屋，窝在桌子前一点点的翻译。虽然都能看懂但要用中文写出来而且语句通顺对我来说还是件很难的事情，前期劲头还很足，后期实在是越翻越无聊，总感觉书的内容太浅，翻译又慢，太浪费宝贵的时间了，后来就叫来达达跟我一起翻译，有了达达的帮忙这本书才终于能够出版了。拿到样书的那一刻还是很激动的。 事实证明初中英语水平也是能翻译技术书的 ：）</p>
<hr>
<p>【原 书 名】 ActionScript 3.0: Visual QuickStart Guide</p>
<p>【原出版社】 Peachpit Press</p>
<p>【作　　者】(加)Derrick Ypenburg</p>
<p>【译　　者】 陈新;吴国权</p>
<p>【丛 书 名】 图灵程序设计丛书</p>
<p>【出 版 社】 人民邮电出版社 </p>
<p>【书 号】 9787115215451</p>
<p>【出版日期】 2009 年11月</p>
<p>【开 本】 16开</p>
<p>【页 码】 226</p>
<p>【版 次】1-1</p>
<hr>
<h3>购买渠道：</h3>
<p><a href="http://product.china-pub.com/196135?ref=xilie" target="_blank">china-pub</a></p>
<p><a href="http://www.amazon.cn/mn/detailApp/ref=sr_1_1?_encoding=UTF8&s=books&qid=1258449105&asin=B002WJI7UW&sr=8-1" target="_blank">卓越</a></p>
<p><a href="http://product.dangdang.com/product.aspx?product_id=20724689" target="_blank">当当</a></p>
]]></content>
    <category scheme="http://nshen.net/tags/翻译/" term="翻译"/>
    <category scheme="http://nshen.net/categories/project/" term="project"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[NTV：基于Flash Media Server的视频文件直播]]></title>
    <link href="http://nshen.net/project/2009-01-13/ntv/"/>
    <id>http://nshen.net/project/2009-01-13/ntv/</id>
    <published>2009-01-13T06:56:31.000Z</published>
    <updated>2013-06-22T05:56:14.000Z</updated>
    <content type="html"><![CDATA[<p>之前的公司有需求做事先录制好的视频文件进行直播，由于Flash Media Server没有类似Windows Media Server的视频文件直播功能，所以我做了这个直播flv文件的傻瓜功能。<a name="more"></a>注意是<strong>直播Flv视频文件</strong>，不是直播摄像头，直播摄像头请找官方的FME，或者<a href="http://nshen.net/article/2007-08-29/fms-tutorial/" target="_blank">这篇教程</a>有实现一个简单的。</p>
<hr>
<p>下载地址：</p>
<p><a href="http://115.com/file/dnh98whr#ntv.rar" target="_blank">NTV 0.2下载（115网盘）</a></p>
<p>注意：此软件<strong>个人网站</strong>与<strong>商业网站</strong>均完全<strong>免费</strong>使用，但请发邮件通知我一下网址。 nshen121[at]gmail.com
好让我评估是否投入精力继续开发此软件。</p>
<hr>
<p>安装方法：</p>
<ol>
<li><p>解压后将ntv文件夹放在 fms安装目录下的applications目录下，例如：
C:\Program Files\Adobe\Flash Media Server 3\applications\ntv </p>
</li>
<li><p>将N2EventDispatcher.asc保存到FMS安装目录下的scriptlib\nfms2文件夹下，例如：
D:\Program Files\Adobe\Flash Media Server 3\scriptlib\nfms2 </p>
</li>
<li><p>修改player.html中的rtmp地址为你fms的ip。</p>
</li>
<li><p>把player和admin文件夹分别传到web可以访问的地方 ，例如：</p>
</li>
</ol>
<p>www.nshen.net/player
www.nshen.net/admin</p>
<h3>使用方法很简单：</h3>
<p>管理端：</p>
<p>打开 <a href="http://你的地址/admin/admin.html">http://你的地址/admin/admin.html</a> 
输入rtmp地址和默认密码 admin （密码在服务器端 fms安装目录\applications\ntv\password.txt 里边改）</p>
<p><img src="/image/ntv01.jpg" alt=""></p>
<p>如果登录成功，左边会自动列出视频库中(ntv\streams_definst_)所有的视频文件 </p>
<p><img src="/image/ntv02.jpg" alt=""></p>
<p>点击播放后，从 播放端 (<a href="http://你的地址/player/player.html">http://你的地址/player/player.html</a>) ,就能看到视频播放了。此时视频是直播的，也就是说所有人看到的是同步的播放。</p>
<p><img src="/image/ntv03.jpg" alt="定时直播功能"></p>
<hr>
<h3>常见问题：</h3>
<p>问:要放什么格式的视频？</p>
<p>答:目前只支持.flv格式的视频。</p>
<p>问:为什么不能定时播放不好用？</p>
<p>答:可能是没有将N2EventDispatcher.asc保存到FMS安装目录下的scriptlib\nfms2文件夹下.</p>
<hr>
<h3>版本历史：</h3>
<p>目前已完全停止开发</p>
<p>ntv 0.2 : 2009-01-03</p>
<ul>
<li>预订时间播放 </li>
<li>停止播放</li>
<li>服务器端使用password.txt来修改密码（密码修改后请reload应用程序，密码生效）</li>
</ul>
<p>ntv 0.1 : 2008-12-24</p>
<ul>
<li>最初版本,实现基本功能</li>
</ul>
]]></content>
    <category scheme="http://nshen.net/categories/project/" term="project"/>
  </entry>
  <entry>
    <title type="html"><![CDATA[Flash Media Server 入门教程]]></title>
    <link href="http://nshen.net/article/2007-08-29/fms-tutorial/"/>
    <id>http://nshen.net/article/2007-08-29/fms-tutorial/</id>
    <published>2007-08-29T06:56:31.000Z</published>
    <updated>2013-06-21T07:29:47.000Z</updated>
    <content type="html"><![CDATA[<p>我把我以前的那些FMS学习笔记整理了一下，做了下修改，成了一页的FMS教程，方便想学FMS的新手来看，如果还有错误或者看不懂的地方欢迎留言.</p>
<a name="more"></a> 

<h3>注意：</h3>
<p>Flash Media Server的最新版本已经到FMS 4.X了 ，由于本入门教程是在2006~2007年所写，年代久远所以使用FMS 2演示，但当你了解了原理后可以很容易的升到4，因为FMS 1 到 FMS 4 的更新几乎没有什么改动 ，还有本教程客户端使用AS1，如果你需要用AS3的话，语法会有些变化，这些都应该难不倒你。</p>
<h3>什么是Flash Media Server ？</h3>
<p>Flash大家庭里的一员，这个东东以前叫Flash Communication Server，传说中的FCS就是这个，现在改叫FMS了...
不见不知道哦，一见真可怕，adobe还有这么cool的东东，自从见了她，让我做些小东东的兴趣全没了，一心想研究这个</p>
<h3>这东东能做什么？</h3>
<p>视频录制啊，flash聊天室啊，在线视频会议啊啊， 网络游戏？</p>
<h3>不管怎样先安个试试吧</h3>
<p>先到这里下载免费的开发者版本吧</p>
<p><a href=""><a href="http://www.macromedia.com/software/flashmediaserver/">http://www.macromedia.com/software/flashmediaserver/</a></a></p>
<p>然后安装，安装时记得用户名和密码不要瞎填自己要记住，如果你已经瞎填了那就到 安装目录下\conf\fms.ini 里找吧 </p>
<p>偶装到了c盘，找到目录 <strong>C:\Program Files\Macromedia\Flash Media Server 2</strong> </p>
<p><img src="/image/fmsTutorial/1.jpg" alt=""></p>
<h3>先要打开服务器哦</h3>
<p>你在本机装的fms，本机就是服务器了哦！记得现在你的机器既是客户端又是服务端哦
开始===程序===Macromedia===Flash Media Server 2
有两个start<em>**</em>,都要打开，关时候两个都要关，如果你怕麻烦直接找tool文件夹里的批处理文件StartServerService.bat吧，双击他，他会帮你搞定地,想关就找StopServerService.bat。~ </p>
<p>如果想设置默认开机时候fms自动打开还是需要手动打开，可以在 开始菜单里--运行--输入services.msc ，就会打开服务，找到Flash Media Server项，设置为手动或者自动，这里也可以随时关闭服务或打开服务</p>
<h3>applications文件夹</h3>
<p>你可以在里边建一个文件夹例如叫 FirstApp，这就建了一个Application，以后所有此项目服务器端的flv文件啊，共享文件啊都会在这里边，有时服务器端需要写程序的时候里边会有个main.asc 文件，这个就是服务器端程序，服务器端程序目前只支持as1写，是在服务器上执行的，你也可以用trace调试，怎么trace管理服务器时候你会看到，你可能会建若干个app，在一台服务器上同时运行，比如录象的app，聊天室的app。。。这些所有的app都通过fms自带的fms2_console.swf管理，现在就去看看如何管理服务器吧！</p>
<h3>管理服务器~</h3>
<p>服务器打开后就要管理了哦，点开fms2_console.swf，别看他只是个swf，后台管理就靠他了，输入你安装时候的密码和用户，服务器地址写 localhost 就好看到了吧，熟悉熟悉他吧</p>
<p><img src="/image/fmsTutorial/2.jpg" alt=""></p>
<p>以后我们写程序最常用的就是这个 <strong>View Applications</strong> ，每当有客户端连接服务器时，左边就会列出连接的是哪个app，有多少连接 下图为连接applications文件夹下的bs文件夹(bs文件夹，因为每个文件夹对应一个app，就是bs app)，连接数为1，458是实例名（实例名默认为 <em>definst</em> ，每个文件夹可以有若干实例，互相不影响，这个特性可以用来做聊天是的房间，以后再说） </p>
<p><img src="/image/fmsTutorial/3.jpg" alt=""></p>
<p>选中某个app后，或者客户端有连接，会看到当前打开的app的状态，这个</p>
<p><img src="/image/fmsTutorial/4.jpg" alt=""></p>
<p>Live Log 服务器端的trace就这里看了 ，右边依次是客户端情况 ， 共享对象，流，执行的情况（占内存，cpu等），后边两个小按钮，调试时候常用哦，reload和unload! 
每当服务器端main.asc修改后一定记得reload或者unload一下，否则不会生效，unload会把窗口关闭，有客户端连的时候还会自动打开 
总有人告诉我，他的fms经常会出现诡异现象，比如连接不上，代码已经删了还会执行，一生气连文件夹都删了，还会执行，怀疑是自己的rp有问题。。 
那不是rp问题，记住出现问题就reload ！实在不行就去tools文件夹点StopServerService.bat</p>
<h3>conf文件夹</h3>
<p>还有重要的是conf文件夹了，里边是一些服务器端的配置文件，以后可能会用，先不用动。。。。 </p>
<p>自此，安装部分结束，go on... </p>
<h3>有一种协议叫rtmp</h3>
<p>客户端和服务器端通信是使用协议rtmp的</p>
<p>现在在服务器端applications文件夹（当然偶的客户端和服务器端是一台机器了）里建个test1文件夹，你的地址就为</p>
<p>rtmp:/test1 或者 rtmp://localhost/test1</p>
<p>注意两个地址中的 &quot;/ &quot;符号</p>
<p>打开flash</p>
<p>与服务器通信首先要建个<strong>NetConnection</strong></p>
<p>nc = new NetConnection(); </p>
<p>连接 </p>
<p>nc.connect(&quot;rtmp://localhost/test1&quot;); </p>
<p>怎么知道连没连上呢？顺利连接服务器后会触发一个onStatus事件,自己trace一下info.code </p>
<p>nc.onStatus = function(info) 
{
    //trace(info)
    //trace(info.code)
    for (i in info) {
    trace(i+&quot;: &quot;+info[i]);
    }
}; </p>
<p>完整代码：</p>
<pre><code><figure class="highlight"><pre>nc = new NetConnection()<span class="comment">;</span>
nc<span class="preprocessor">.onStatus</span> = function(info) 
{
     trace(info<span class="preprocessor">.code</span>)<span class="comment">;</span>
     if (info<span class="preprocessor">.code</span> == <span class="string">"NetConnection.Connect.Success"</span>) {
         trace(<span class="string">"接通"</span>)<span class="comment">;</span>
     }
}<span class="comment">;</span>
nc<span class="preprocessor">.connect</span>(<span class="string">"rtmp://localhost/test1"</span>)<span class="comment">;</span>
</pre></figure></code></pre>
<h4>注意：</h4>
<p>默认情况下服务器是允许你连接的，但只是默认，如果服务器拒绝你连接的话，上述代码就不好用了。怎么回事？我们看看连接的过程吧</p>
<h3>连接过程</h3>
<p>每当客户端试图连接服务器，一个NetConnection.connect()，服务器将会调用application.onConnect 来鉴定是不是允许客户端连接，onConnect()方法返回null 或不返回则将进入未决状态,直到onConnect方法中返回true或执行acceptConnection(client)则允许，返回false或执行rejectConnection(client)则拒绝，如图(从左往右看)</p>
<p><img src="/image/fmsTutorial/5.jpg" alt=""></p>
<p>服务器文件是以<strong>.asc</strong>文件形式存在的，可以在test1文件夹里建一个<strong>main.asc</strong>。</p>
<pre><code><figure class="highlight"><pre>application.onConnect=function(<span class="keyword">client</span>)
{
    <span class="keyword">this</span>.rejectConnection(<span class="keyword">client</span>);        
    <span class="comment">//this.acceptConnection(client)</span>
} 
</pre></figure></code></pre>
<p>这样就拒绝连接了，动手试试，别忘了，服务器端改动的话，别忘了到fms2_console.swf去reload ！不知道按哪个的到上边找，每当有客户端连接，那个reload按钮的界面就会出来哦。</p>
<h3>检查是否uri错误</h3>
<p>如果你的rtmp地址是从其他什么地方传过来的，可以顺便检查一下rtmp是否错误，下边代码如果地址是错误的就会trace出来。</p>
<pre><code><figure class="highlight"><pre>mync = new NetConnection()<span class="comment">;</span>
mync<span class="preprocessor">.onStatus</span> = function(info) 
{
    if (info<span class="preprocessor">.code</span> == <span class="string">"NetConnection.Connect.Success"</span>) 
    {
         trace(<span class="string">"连接成功"</span>)<span class="comment">;</span>
     }
}<span class="comment">;</span>
//正确的uri
//uri = <span class="string">"rtmp://localhost/connect"</span><span class="comment">;</span>
//错误的uri
uri = <span class="string">"rtmpppppp://localhost/connect"</span><span class="comment">;</span>

if (mync<span class="preprocessor">.connect</span>(uri, <span class="string">"N神"</span>)) {
 trace(<span class="string">"尝试连接服务器中。。"</span>)<span class="comment">;</span>
} else {
 trace(<span class="string">"没有尝试连接服务器~是uri错误？？？"</span>)<span class="comment">;</span>
}
</pre></figure></code></pre>
<p>ok 了，现在我们深入一点点。。看看连接上的一些细节问题</p>
<h3>info.code：</h3>
<p>连接后info.code会告诉你连接的状态，上边看的都是<strong>NetConnection.Connect.Success</strong>， 还有一些其他值，和这些值是什么意思，自己看看。</p>
<p>值得注意的是。<strong>NetConnection.Connect.Rejected</strong>，收到这条消息的时候说明服务器端拒绝了你，接着马上你会收到另一条，<strong>NetConnection.Connect.Closed</strong>，连接就关闭了。</p>
<pre><code><figure class="highlight"><pre>mync = new NetConnection()<span class="comment">;</span>
mync<span class="preprocessor">.onStatus</span> = function(info) 
{
 switch (info<span class="preprocessor">.code</span>) 
{
     case <span class="string">"NetConnection.Connect.Success"</span> :
         trace(<span class="string">"连接成功"</span>)<span class="comment">;</span>
     <span class="keyword">break</span><span class="comment">;</span>
     case <span class="string">"NetConnection.Connect.Failed"</span> :
         //关掉服务器的情况
         trace(<span class="string">"尝试连接失败，服务器有可能挂掉了 -_-b"</span>)<span class="comment">;</span>
     <span class="keyword">break</span><span class="comment">;</span>
     case <span class="string">"NetConnection.Connect.Rejected"</span> :
         //注意这里，服务器拒绝你的情况，如果遭到拒绝，将会调用两次mync<span class="preprocessor">.onStatus</span>，
          //第一次<span class="string">"NetConnection.Connect.Rejected"</span>第<span class="number">2</span>次<span class="string">"NetConnection.Connect.Closed"</span>
         trace(<span class="string">"遭到服务器拒绝"</span>)<span class="comment">;</span>
         trace(<span class="string">"服务器返回信息："</span>+info<span class="preprocessor">.application</span><span class="preprocessor">.msg</span>)<span class="comment">;</span>
     <span class="keyword">break</span><span class="comment">;</span>
         case <span class="string">"NetConnection.Connect.Closed"</span> :
         trace(<span class="string">"连接关闭"</span>)<span class="comment">;</span>
     <span class="keyword">break</span><span class="comment">;</span>
 }
}<span class="comment">;</span>
mync<span class="preprocessor">.connect</span>(<span class="string">"rtmp://localhost/connect"</span>, <span class="string">"N神"</span>)<span class="comment">;</span>
//mync<span class="preprocessor">.connect</span>(<span class="string">"rtmp://localhost/connect"</span>,<span class="string">"小新"</span>)
</pre></figure></code></pre>
<p>服务器端拒绝连接？好象见过。。。回头找找。。。。。。。。哦在这里</p>
<pre><code><figure class="highlight"><pre>application.onConnect = function(<span class="keyword">client</span>) 
{    
    <span class="keyword">this</span>.rejectConnection(<span class="keyword">client</span>);    
}
</pre></figure></code></pre>
<p>我不能所有人都拒绝了。。我要把讨厌的人拒绝了。。。
传给服务器一个人名~~</p>
<pre><code><figure class="highlight"><pre>mync<span class="preprocessor">.connect</span>(<span class="string">"rtmp://localhost/connect"</span>, <span class="string">"N神"</span>)<span class="comment">;</span>
</pre></figure></code></pre>
<p>服务器看看是不是讨厌的人。。</p>
<pre><code><figure class="highlight"><pre><span class="type">application</span>.onConnect = function(client, <span class="property">name</span>) {
 trace(<span class="property">name</span>);
 <span class="keyword">if</span> (<span class="property">name</span> == <span class="string">"N神"</span>) {
     //拒绝连接，并返回个错误对象{msg:<span class="string">"服务器不想"</span>+<span class="property">name</span>+<span class="string">"进去，哈哈~"</span>}，包含错误消息
     <span class="type">application</span>.rejectConnection(client, {msg:<span class="string">"服务器不想"</span>+<span class="property">name</span>+<span class="string">"进去，哈哈~"</span>});
 } <span class="keyword">else</span> {
     <span class="type">application</span>.acceptConnection(client);
     //成功不能返回客户端信息
 }
};
</pre></figure></code></pre>
<p>看客户端的代码。。 </p>
<pre><code><figure class="highlight"><pre><span class="keyword">case</span> <span class="string">"NetConnection.Connect.Rejected"</span> :
//注意这里，服务器拒绝你的情况，如果遭到拒绝，将会调用两次mync.on<span class="variable">Status</span>，
//第一次<span class="string">"NetConnection.Connect.Rejected"</span>第<span class="number">2</span>次<span class="string">"NetConnection.Connect.Closed"</span>
<span class="function"><span class="title">trace</span><span class="params">(<span class="string">"遭到服务器拒绝"</span>)</span>;
<span class="title">trace</span><span class="params">(<span class="string">"服务器返回信息："</span>+info.application.msg)</span>;
<span class="title">break</span>; 
</pre></figure></code></pre>
<p>遭到服务器拒绝后会trace出服务器返回的错误消息，这是一个最基础的与服务器交互的例子，以后还会有很多滴 </p>
<p>下边进入新一层次。。。 </p>
<h3>视频，流</h3>
<p>这个比较重要，但超简单，网上播放电影，在线录制，在线播放，视频会议，视频电话，全靠他了 ，下边我们先做一个最简单 录制和播放 </p>
<h4>录制flv视频</h4>
<p>打开flash，新建一个fla，Ctrl + L 打开library，右键新建视频，确定。 </p>
<p>拉到舞台上起个名叫my_video </p>
<p>第一帧开始加代码 </p>
<pre><code><figure class="highlight"><pre>//从麦和设像头显示视频显示在my_video上     
my_video<span class="preprocessor">.attachVideo</span>(Camera<span class="preprocessor">.get</span>())<span class="comment">;</span>
my_video<span class="preprocessor">.attachAudio</span>(Microphone<span class="preprocessor">.get</span>())<span class="comment">; </span>

//像以前一样连接 
nc = new NetConnection()<span class="comment">;</span>
nc<span class="preprocessor">.connect</span>(<span class="string">"rtmp://localhost/aaaa"</span>)<span class="comment">; //注意这里，Applications文件夹里要有aaaa文件夹哦！ </span>


//可以理解为在nc连接上绑一个流 
nsOut = new NetStream(nc)<span class="comment">; </span>

//在流上加麦克风和视频头     
nsOut<span class="preprocessor">.attachVideo</span>(Camera<span class="preprocessor">.get</span>())<span class="comment">;</span>
nsOut<span class="preprocessor">.attachAudio</span>(Microphone<span class="preprocessor">.get</span>())<span class="comment">; </span>

//发布 <span class="number">2.</span>flv
nsOut<span class="preprocessor">.publish</span>(<span class="string">"2"</span>, <span class="string">"record"</span>)<span class="comment">; </span>
</pre></figure></code></pre>
<p>第一个参数是文件名，后一个参数要&quot;record&quot;才是录制。</p>
<p>把fla发布一下， 录一会儿，把视频关掉，打开你的</p>
<blockquote>
<p>叉盘:\Program Files\Macromedia\Flash Media Server 2\applications\aaaa\</p>
</blockquote>
<p>是不是多了个<strong>streams_definst_</strong></p>
<p>打开</p>
<blockquote>
<p>C:\Program Files\Macromedia\Flash Media Server 2\applications\aaaa\streams_definst_</p>
</blockquote>
<p>看见2.flv了吧。</p>
<p><a href="/image/fmsTutorial/fla1.fla" title="下载源文件 fla1.fla">这里下载原文件 fla1.fla</a></p>
<h4>播放flv视频</h4>
<p>用fms播放的flv目前是无法下载的，这可以保护你的版权 ：） </p>
<p>打开flash，新建一个fla ，Ctrl + L 打开library，右键新建视频，确定。 </p>
<p>拉到舞台上，这回起个名叫view ，我们来播放你刚才录的那个视频 </p>
<pre><code><figure class="highlight"><pre>nc = new NetConnection()<span class="comment">;</span>
nc<span class="preprocessor">.connect</span>(<span class="string">"rtmp://localhost/aaaa"</span>)<span class="comment">;</span>
res = new NetStream(nc)<span class="comment">; </span>

//view元件要加载res流     
view<span class="preprocessor">.attachVideo</span>(res)<span class="comment">;</span>
view<span class="preprocessor">.attachAudio</span>(res)<span class="comment">; </span>

//播放     
res<span class="preprocessor">.play</span>(<span class="string">"2"</span>)<span class="comment">; </span>
</pre></figure></code></pre>
<p>[/image/fmsTutorial/fla2.fla](这里下载原文件 fla2.fla) </p>
<h3>现场流</h3>
<p>上边做的都是先录制好了视频，然后才播放，网上的实时视频会议，视频电话是怎么做的呢？总不能先录好再播放吧？ 
这要用到现场流，现场流是指你连接到服务器后，你在发布的同时，其他人就可以实时的看到你
很难吗？看看吧，把上边录制视频的例子拿下来 </p>
<pre><code><figure class="highlight"><pre>nsOut<span class="preprocessor">.publish</span>(<span class="string">"2"</span>, <span class="string">"record"</span>)<span class="comment">; </span>
</pre></figure></code></pre>
<p>这句改成 </p>
<pre><code><figure class="highlight"><pre>nsOut<span class="preprocessor">.publish</span>(<span class="string">"2"</span>, <span class="string">"live"</span>)<span class="comment">; </span>
</pre></figure></code></pre>
<p>把&quot;record&quot; 改成&quot;live&quot;后，就不会生成flv了，取而代之的是一个看不到的实时的视频流 
ok 了，发布，这就是直播端了，同时再发布上边那个播放端，已经可以实时看见你了吧。现在你是用本机测试，等你有了服务器。其他人也能同时看见你了哦。</p>
<p>到这里你已经可以自己做一个网页上的直播了，发布端不要让别人看到，让别人看你的播放端就好了 ：） 至于为什么要用两个swf，因为目前为止你还不知道怎么样跟服务器之间传递消息，这样做可以避免这些东西，等你把后边的东西全都学完就可以在一个swf里，写个视频会议之类的东东了。</p>
<h3>远程共享</h3>
<p>远程共享？</p>
<p>共享的概念就是让每个连接到服务器的swf都能实时的得到服务器端共享的数据。
一个人更改了这些数据，其他人都会看得到通知。可以想象聊天室里的发言，一个人发了以后其他人都可以看到。</p>
<p><img src="/image/fmsTutorial/6.jpg" alt=""></p>
<h3>共享对象</h3>
<p>共享对象，说英文大概你比较熟ha~ SharedObject， 恩flash中有两种sharedObject，local sharedobject （LSO） 和 remote sharedobject (RSO)，也就是本地共享和远程共享,偶们讨论远程的，不过之前你最好先去了解了解本地的，对你有好处...</p>
<p>RSO在服务器端是以文件形式存储的，扩展名是.fso，为什么不是.rso?....我也想问呢- -b</p>
<h3>代码</h3>
<p>初始化RSO需要先与服务器建立一个连接，续上节 ，我们先与服务器建立一个连接 </p>
<pre><code><figure class="highlight"><pre><span class="comment">//初始化远程共享要利用nc通道</span>
<span class="keyword">var</span> myNC = <span class="keyword">new</span> NetConnection();
myNC.onStatus = <span class="keyword">function</span>(info) {
 <span class="keyword">if</span> (info.code == <span class="string">"NetConnection.Connect.Success"</span>) {
 <span class="comment">//成功则利用此nc初始化rso</span>
 initRSO(<span class="keyword">this</span>);
 }
};
</pre></figure></code></pre>
<p>跟以前的代码一样，只是连接成功后多了一个initRSO()函数，看不懂的回去重头再看一下。。。</p>
<p>下边是initRSO了，跟连接结构差不多 </p>
<pre><code><figure class="highlight"><pre><span class="function"><span class="keyword">function</span> <span class="title">initRSO</span><span class="params">(NC)</span> 
{</span>
     <span class="comment">//在服务器上建立myRSO.fso文件，第2个参数指定nc通道，第3个指定文件在服务器上持久保留，即使服务器重启了，还是有</span>
     my_rso = SharedObject.getRemote(<span class="string">"myRSO"</span>, NC.uri, <span class="literal">true</span>);
     my_rso.onSync = <span class="keyword">function</span>(list) {
     <span class="comment">//list 是一个对象数组 ，类似这种[{name:"x",code:"success"},{name:"y",code:"success"}] ，下边会详细讲</span>
     <span class="comment">//初始成功</span>
     };
     my_rso.connect(NC); <span class="comment">//连接</span>
}
</pre></figure></code></pre>
<p>了解了吧，看一个完整的例子</p>
<p>画一个mc起名叫mc，在第一帧上写代码</p>
<pre><code><figure class="highlight"><pre><span class="comment">//初始化远程共享要利用nc通道</span>
var myNC = new NetConnection();
myNC<span class="variable">.onStatus</span> = function(info) {
 <span class="keyword">if</span> (info<span class="variable">.code</span> == <span class="string">"NetConnection.Connect.Success"</span>) {
 <span class="comment">//成功则利用此nc初始化rso</span>
 initRSO(<span class="keyword">this</span>);
 }
};
myNC<span class="variable">.connect</span>(<span class="string">"rtmp://localhost/test1"</span>); <span class="comment">//不会不知道要建test1文件夹吧 </span>

function initRSO(NC) {
 my_rso = SharedObject<span class="variable">.getRemote</span>(<span class="string">"myRSO"</span>, NC<span class="variable">.uri</span>, <span class="literal">true</span>); 
<span class="comment">//onSync是回调函数，每次服务器端so数据有改变，这里都会有反映！这里的意思每当有人按鼠标，这里都会有反映，我们读取so的data下的值就可以了</span>
 my_rso<span class="variable">.onSync</span> = function() {
 mc<span class="variable">._x</span>=<span class="keyword">this</span><span class="variable">.data</span><span class="variable">.x</span>
 mc<span class="variable">._y</span>=<span class="keyword">this</span><span class="variable">.data</span><span class="variable">.y</span>
 };
 my_rso<span class="variable">.connect</span>(NC);
} 

onMouseDown = function () { 
 <span class="comment">//改变so的数据</span>
 my_rso<span class="variable">.data</span><span class="variable">.x</span> = _root<span class="variable">._xmouse</span>
 my_rso<span class="variable">.data</span><span class="variable">.y</span> = _root<span class="variable">._ymouse</span>
};
</pre></figure></code></pre>
<p>然后发布设置中设置<strong>只允许网络</strong>，发布看看</p>
<p>现在你可以开多个播放器窗口，点其中一个，看看其他的窗口变不变?</p>
<p><a href="/image/fmsTutorial/fla3.fla" title="下载源文件 fla3.fla">这里下载原文件 fla3.fla</a></p>
<h3>连接流程</h3>
<p><img src="/image/fmsTutorial/7.jpg" alt=""></p>
<p>再写一个，看起来很像在做网游~</p>
<pre><code><figure class="highlight"><pre>mync = new NetConnection()<span class="comment">;</span>
mync<span class="preprocessor">.onStatus</span> = function(info) {
 if (info<span class="preprocessor">.code</span> == <span class="string">"NetConnection.Connect.Success"</span>) {
 initRSO()<span class="comment">;</span>
 }
 if (info<span class="preprocessor">.code</span> == <span class="string">"NetConnection.Connect.Closed"</span>) {
 trace(<span class="string">"关闭"</span>)<span class="comment">;</span>
 }
}<span class="comment">; </span>

function initRSO() {
 my_RSO = SharedObject<span class="preprocessor">.getRemote</span>(<span class="string">"myRSO"</span>, mync<span class="preprocessor">.uri</span>, true)<span class="comment">;</span>
 trace(my_RSO)<span class="comment">;</span>
 my_RSO<span class="preprocessor">.onSync</span> = function() {
 mc._<span class="built_in">x</span> = this<span class="preprocessor">.data</span><span class="preprocessor">.hero</span><span class="preprocessor">.x</span><span class="comment">;</span>
 }<span class="comment">;</span>
 my_RSO<span class="preprocessor">.connect</span>(mync)<span class="comment">;</span>
}
mync<span class="preprocessor">.connect</span>(<span class="string">"rtmp:/my_app/test1"</span>)<span class="comment">;</span>

mc<span class="preprocessor">.onEnterFrame</span> = function() {
 my_RSO<span class="preprocessor">.data</span><span class="preprocessor">.hero</span><span class="preprocessor">.x</span> = this._<span class="built_in">x</span><span class="comment">;</span>
 if (Key<span class="preprocessor">.isDown</span>(Key<span class="preprocessor">.LEFT</span>)) {
 this._<span class="built_in">x</span> -= <span class="number">5</span><span class="comment">;</span>
 }
 if (Key<span class="preprocessor">.isDown</span>(Key<span class="preprocessor">.RIGHT</span>)) {
 this._<span class="built_in">x</span> += <span class="number">5</span><span class="comment">;</span>
 }
}<span class="comment">;</span>
</pre></figure></code></pre>
<h3>写个简单的聊天室</h3>
<p>很简单的东西，基本上就是共享对象的运用，没有用到服务器端，大型聊天室可能不会这么做，这个只适用于初学者 ：/</p>
<p>注释很详细，不说多了，可以直接下载原文件</p>
<p><a href="/image/fmsTutorial/fla4.fla" title="下载源文件 fla4.fla">这里下载原文件 fla4.fla</a></p>
<p>代码： </p>
<pre><code><figure class="highlight"><pre><span class="comment">//用户名</span>
myname=<span class="string">"游客"</span>
<span class="comment">//建立连接</span>
var myNC = new NetConnection();
myNC<span class="variable">.connect</span>(<span class="string">"rtmp://localhost/smallchat"</span>);

<span class="comment">//搞到rso</span>
Talk_SO = SharedObject<span class="variable">.getRemote</span>(<span class="string">"Talk"</span>, myNC<span class="variable">.uri</span>, <span class="literal">false</span>);
Talk_SO<span class="variable">.onSync</span> = function() {
 <span class="comment">//先把聊天文本框清空</span>
 remoteText<span class="variable">.text</span> = <span class="string">""</span>;
 <span class="comment">//把聊天列表显示出来，talklist的格式就是[谁谁说:啊啊啊,谁谁谁说:2222]</span>
 var t = <span class="keyword">this</span><span class="variable">.data</span><span class="variable">.talklist</span>;
 <span class="keyword">for</span> (var i = <span class="number">0</span>; i&lt;t<span class="variable">.length</span>; i++) {
 writeln(t[i]);
 }
};
Talk_SO<span class="variable">.connect</span>(myNC);

<span class="comment">//发消息函数</span>
function post() {
 <span class="comment">//如果不存在talklist就建一个，这里没用server端，是个技巧</span>
 <span class="keyword">if</span> (Talk_SO<span class="variable">.data</span><span class="variable">.talklist</span>[<span class="number">0</span>] == undefined) {
     Talk_SO<span class="variable">.data</span><span class="variable">.talklist</span> = [];
 }
 <span class="comment">//限制数组长度，是个队列。保证里边有5条消息，当然也可以更多，但如果没有限制，flash会垮的</span>
 <span class="keyword">if</span> (Talk_SO<span class="variable">.data</span><span class="variable">.talklist</span><span class="variable">.length</span>&gt;=<span class="number">5</span>) {
     Talk_SO<span class="variable">.data</span><span class="variable">.talklist</span><span class="variable">.shift</span>();
 }
 <span class="comment">//把消息装到so里</span>
 Talk_SO<span class="variable">.data</span><span class="variable">.talklist</span><span class="variable">.push</span>(myname+<span class="string">"说："</span>+meText<span class="variable">.text</span>);
 meText<span class="variable">.text</span> = <span class="string">""</span>;
}
<span class="comment">//文字显示，换行</span>
function writeln(msg) {
 remoteText<span class="variable">.text</span> += msg+<span class="string">"\n"</span>;
 remoteText<span class="variable">.vPosition</span> =remoteText<span class="variable">.maxVPosition</span>
}
<span class="comment">//-----------------------------------------------</span>
Btn<span class="variable">.onRelease</span> = function() {
 post();
};
<span class="keyword">this</span><span class="variable">.onKeyDown</span> = function() {
 <span class="keyword">if</span> (Key<span class="variable">.isDown</span>(Key<span class="variable">.ENTER</span>)) {
     post();
 }
};
Key<span class="variable">.addListener</span>(<span class="keyword">this</span>);
</pre></figure></code></pre>
<h3>深入onSync</h3>
<p>onSync有个list参数，这个开始有些难度了。不想动脑的可以跳过没影响，只是以后写出的程序效率会低点 ：（</p>
<p>看代码：</p>
<pre><code><figure class="highlight"><pre>my_rso = SharedObject<span class="variable">.getRemote</span>(<span class="string">"myRSO"</span>, NC<span class="variable">.uri</span>, <span class="literal">true</span>);
my_rso<span class="variable">.onSync</span> = function(list) {<span class="comment">//.......};</span>
my_rso<span class="variable">.connect</span>(NC); <span class="comment">//连接</span>
</pre></figure></code></pre>
<p>在onSync回调中我们可以知道我们的my_rso被改变了，但my_rso里具体什么改变了呢？ 我们就要分析这个 list 参数 了</p>
<p>list参数其实是一个对象数组 ，首先它是一个数组，里边装了很多对象(Object)，每一个对象都包括了SharedObject中一个插槽(slot)的改动信息。我暂时给他起名叫插槽信息对象。。。这名字太猥亵了。。但我就这么叫了。。</p>
<p>插槽信息对象包含两个属性，name 和 code，偶尔还会有个oldValue?我不太常用，不说它</p>
<p>name 描述被改变的属性名</p>
<p>code 描述该属性的改变方式 ，有可能为以下几种值：&quot;success&quot; ， &quot;change&quot; ， &quot;delete&quot; ， &quot;reject&quot; ， &quot;clear&quot; ，具体含义后边说</p>
<p>说白了这个插槽信息对象大概就是这么个样子：</p>
<p>{name:&quot;x&quot;,code:&quot;success&quot;} </p>
<p>表示x属性被修改成功</p>
<p>要得到这些插槽信息对象就要for in 这个list参数</p>
<p>for (var i in list) {</p>
<p>list[i] 就是插槽信息对象</p>
<p>}</p>
<p>要分析具体so哪改变了，就是分析list[i]，比如</p>
<p>if(list[i].code==&quot;change&quot;) trace(&quot;list[i].name&quot;+被+&quot;change了&quot;)</p>
<p>if(list[i].code==&quot;delete&quot;) trace(&quot;list[i].name&quot;+被+&quot;delete&quot;)</p>
<p>“change”是啥？“delete”是啥？</p>
<p>&quot;success&quot; ， &quot;change&quot; ， &quot;delete&quot; ， &quot;reject&quot; ， &quot;clear&quot; 具体含义：</p>
<p>success : 表示当前影片修改so的插槽获得了成功</p>
<p>change : 表示so的插槽被别人修改，或填加</p>
<p>也就是说，你修改so的某个属性成功了会收到 &quot;success&quot; ,与此同时其他影片会收到 &quot;change&quot;</p>
<p>reject : 拒绝修改</p>
<p>例如发生在两个或多个客户端同时要修改一个so的插槽，这时候fms会只让一个client修改，并返回&quot;success&quot; 其他的会收到&quot;reject&quot;</p>
<p>delete , clear : 这个好理解，一个是删除，一个是清空，看例子：</p>
<p>比如服务器端删除某个so</p>
<pre><code><figure class="highlight"><pre>so = SharedObject<span class="preprocessor">.get</span>(<span class="string">"某个so"</span>)<span class="comment">;</span>
so<span class="preprocessor">.lock</span>( )<span class="comment">;</span>
var names = so<span class="preprocessor">.getPropertyNames</span>( )<span class="comment">;</span>
for (i <span class="keyword">in</span> names) {
 so<span class="preprocessor">.setProperty</span>(names[i], null)<span class="comment">;</span>
}
so<span class="preprocessor">.unlock</span>( )<span class="comment">;</span>
</pre></figure></code></pre>
<p>这样client端会收到 若干个插槽信息对象，所有的code都为&quot;delete&quot;，表示若干个item被删除</p>
<p>然而这样：</p>
<pre><code><figure class="highlight"><pre>so = SharedObject<span class="preprocessor">.get</span>(<span class="string">"某个so"</span>)<span class="comment">;</span>
so<span class="preprocessor">.clear</span>( )<span class="comment">;</span>
</pre></figure></code></pre>
<p>client端就只会收到一个插槽信息对象，code属性为“clear”。 </p>
<h3>client端与server端直接交互</h3>
<p>看完了SO，看一下client与server端如何直接进行交互的 </p>
<p><a href="/image/fmsTutorial/1.rar" title="下载源文件 1.rar">这里下载原文件 1.rar</a></p>
<h4>1. 客户端呼叫服务器</h4>
<p>fla： </p>
<pre><code><figure class="highlight"><pre>//客户端呼叫server端msgfromclient函数,并将返回值trace出来
mync = new NetConnection()<span class="comment">;</span>
mync<span class="preprocessor">.connect</span>(<span class="string">"rtmp://localhost/connect"</span>)<span class="comment">;</span>
//返回值接收对象
var resObj = new Object()<span class="comment">;</span>
resObj<span class="preprocessor">.onResult</span> = function(val):Void {
 trace(<span class="string">"val"</span>+val)<span class="comment">;</span>
}<span class="comment">; </span>
<span class="comment">/* 
我们用mync去call服务器端的msgfromclient函数，resObj是返回接收对象，当服务器有返回值后，会自动直接调用这个对象的onResult处理函数，后边可以传递给server无数个参数，这里只传一个字符串 
*/</span>
mync<span class="preprocessor">.call</span>(<span class="string">"msgfromclient"</span>, resObj, <span class="string">"第一个call"</span>)<span class="comment">;</span>
</pre></figure></code></pre>
<p>服务器端代码是放在main.asc里的，你可以到你的application下的connect目录下建一个main.asc，写代码</p>
<p>main.asc: </p>
<pre><code><figure class="highlight"><pre><span class="regexp">//</span>要把函数定义到Client上！！
application.onConnect = <span class="reserved">function</span>(client) {
    /* 在这里定义也可以，在Client.prototype里定义也是可以的 
    client.msgfromclient=<span class="reserved">function</span>(what){
         trace(what+<span class="string">"进来了"</span>)            
         <span class="reserved">var</span> aa=<span class="string">"呼叫成功并返回结果"</span>
         <span class="keyword">return</span> aa
     }
 */
    application.acceptConnection(client);
};

Client.prototype.msgfromclient=<span class="reserved">function</span>(what){
     trace(what+<span class="string">"进来了"</span>)

     <span class="reserved">var</span> aa=<span class="string">"呼叫成功并返回结果"</span>
     <span class="keyword">return</span> aa
}
</pre></figure></code></pre>
<p>现在去试一下吧。。。成功了的话，再继续....... </p>
<h4>2. 服务器端呼叫指定的客户端</h4>
<p>fla： </p>
<pre><code><figure class="highlight"><pre>//server呼叫client端
//要把函数定义到nc上！！
//
mync = new NetConnection()<span class="comment">;</span>
mync<span class="preprocessor">.onStatus</span> = function(info) {
 if (info<span class="preprocessor">.code</span> == <span class="string">"NetConnection.Connect.Success"</span>) {
 trace(<span class="string">"连接成功"</span>)<span class="comment">;</span>
 }
}<span class="comment">;</span>
mync<span class="preprocessor">.connect</span>(<span class="string">"rtmp://localhost/connect"</span>)<span class="comment">;</span>
mync<span class="preprocessor">.msgfromserver</span> = function(msg) {
 trace(msg)<span class="comment">;</span>
}<span class="comment">;</span>
</pre></figure></code></pre>
<p>main.asc： </p>
<pre><code><figure class="highlight"><pre>application.onConnect = function(<span class="keyword">client</span>) {     
    application.acceptConnection(<span class="keyword">client</span>); 
    <span class="comment">//这里呼叫刚连线成功的客户 </span>
    <span class="comment">//跟client呼叫server基本一样，服务器一般很少让client端返回值所以第2个参数设为null</span>
    <span class="keyword">client</span>.call(<span class="string">"msgfromserver"</span>,<span class="keyword">null</span>,<span class="string">"服务器叫你啊"</span>)
};
</pre></figure></code></pre>
<h4>3. 服务器端呼叫所有的客户端(广播)</h4>
<p>有些时候需要服务器广播数据给所有连接上的客户端，这里就用到了广播的概念 </p>
<p>广播其实SharedObject的时候已经讲过了一种实现，就是把数据放到remote SharedObject中，当数据改变了，自然所有客户端都会onSync 
这里再讲一种用call来实现的： </p>
<p>下边是很常见的一个情况，当某人下线了的时候要通知所有客户端，某某已经下线了 </p>
<p>server端:</p>
<pre><code><figure class="highlight"><pre>application<span class="variable">.onDisconnect</span>=function(newClient){ 
    <span class="comment">//遍历客户端列表，分别call他们</span>
    <span class="keyword">for</span>(var i=<span class="number">0</span>;i&lt;application<span class="variable">.clients</span><span class="variable">.length</span>;i++) {
        application<span class="variable">.clients</span>[i]<span class="variable">.call</span>(<span class="string">"client_fun"</span>，null，sendvar);
    }
}
</pre></figure></code></pre>
<p>Client端:</p>
<pre><code><figure class="highlight"><pre><span class="comment">nc</span>.<span class="comment">client_fun=function(myvar){</span>.<span class="string">.</span><span class="string">.</span><span class="string">.</span><span class="string">.</span><span class="string">.</span><span class="string">.</span> <span class="comment">}</span> 
</pre></figure></code></pre>
<p>这个自己完善一下吧，这里就不贴fla了 </p>
<p>还有： </p>
<p>关于广播，不只有服务器端广播给所有客户，还有可能某一个客户端对所有客户端直接进行广播，当然上边的例子你如果都看懂了的话，你已经可以自己做某一个客户端对所有客户端的广播了。怎么做？ </p>
<ul>
<li>第1步 某一个客户端呼叫服务器 </li>
<li>第2步 服务器广播给所有客户端 </li>
</ul>
<p>这样就形成了，某客户端对所有客户端的广播，当然如果你能细心的耐心的看看帮助的话，你会发现Shared Object 和 NetStream都有send方法就是做这件事的，而且更为简洁，服务端不用写代码 ：） </p>
<p>好了，看到这里，fms常用的大部分概念都说到了，这个教程也就基本结束了，但请注意,现在你只是入门阶段.想学更多的，你可能需要多翻翻手册，多找找教程，英文有不少很好的教程进阶. </p>
<h3>其他需要注意的问题：</h3>
<h4>中文编码：</h4>
<p>有些时候我们用flash去读取外部的php，asp.....文件里的中文显示在flash里会出现乱码的情况，为了解决在flash里显示中文很多教程里通常都直接加了一句System.useCodepage=true 
问题就在这，显示不了外部中文是因为flash内默认用Unicode编码，外部的文件大多都是gb2312,加上这句System.useCodepage=true代表强制flash使用系统默认的gb2312，这样flash就显示正确了，但fms服务器端默认也是用unicode的，这样客户端跟服务器端不同编码有时就会出错了，搜了一下server字典好象没有System.useCodepage=true了。。。所以解决办法就是去掉System.useCodepage=true，在外部php或asp中把编码转成utf-8，至于怎么转，不知道，问你的asp或php程序员吧 ，另外不要用记事本编辑你的asc文件。。即使编辑最后要一定另存为utf-8格式。</p>
<h4>判断影片播放结束</h4>
<p>ns.onStatus=function(info){
    if(info.code==&quot;NetStream.Play.Stop&quot;)trace(&quot;结束&quot;)
}</p>
<p>乍看好象没错，但是如果设置了缓冲以后（setBufferTime）就不好用了，仔细研究了一下原因4这样的</p>
<p>监视onStatus(info) ，info.code:</p>
<p>开始播放
NetStream.Play.Start （其实还没播放）</p>
<p>然后缓冲（根据setBufferTime设置的秒数缓。。）</p>
<p>NetStream.Buffer.Full （缓冲装满了，这才开始播放）</p>
<p>然后播放完了NetStream.Play.Stop （其实还没播放完）</p>
<p>注意了，然后还要播放缓冲 - -b
NetStream.Buffer.Empty （缓冲空了，这才播放完了。。）</p>
<p>群里的kinglong兄比较聪明~，先Stop的时候做个记号，然后再满足Empty才算播放完，也就是两个条件，因为网速慢也会Empty。。。好办法。</p>
<p>我后来又看了一下帮助，好象有一个专门的事件通知播放结束
ns.onPlayStatus=function(info){
    if(info.code==&quot;NetStream.Play.Complete&quot;)trace(&quot;感谢观看帮助&quot;)
}</p>
<h4>防火墙，端口</h4>
<p>默认安装的话默认端口是1935，管理是1111端口，记得防火墙要把1935和1111端口打开。</p>
<p>本文原地址：<a href="http://www.nshen.net/article/2007/08/29/fms-tutorial/">http://www.nshen.net/article/2007/08/29/fms-tutorial/</a> ,转载请保留链接 。</p>
]]></content>
  </entry>
</feed>
